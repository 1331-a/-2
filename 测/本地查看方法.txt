cd "C:/Users/HP/WorkBuddy/德扑双人机器人测试/内容"

# 1) 看内置演示（3 个典型场景，不需要任何输入）
python view_log.py --demo

# 2) 重放某局 botzone 对局（把对局详情里的 request 存成 JSON）
python view_log.py payload.json

# 3) 只看特定手数
python view_log.py payload.json --hand 12 30

# 4) 落盘成 JSON 便于统计
python view_log.py payload.json --json out.json
