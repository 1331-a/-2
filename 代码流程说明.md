# 德州扑克机器人 —— 从平台输入到返回的完整流程说明

> 面向 Python 初学者。读完你应该能回答：
> **平台发来的一串 JSON，是怎么变成"弃牌 / 跟注 / 加注 300"这个整数的？**
>
> 全部源码在 `内容/` 目录。本文只讲**主干流程**，不展开策略数学细节。

---

## 目录

1. [整体地图：谁在跟谁说话](#一整体地图谁在跟谁说话)
2. [一轮交互的完整时序（10 步）](#二一轮交互的完整时序10-步)
3. [第一层：`bot.py` —— 收发消息的门卫](#三第一层botpy--收发消息的门卫)
4. [第二层：`game_state.py` —— 把 JSON 翻译成"牌局状态"](#四第二层game_statepy--把-json-翻译成牌局状态)
5. [第三层：`opponent.py` —— 给对手建档案](#五第三层opponentpy--给对手建档案)
6. [第四层：`match_ctx.py` —— 记住"这是第几局、什么赛制"](#六第四层match_ctxpy--记住这是第几局什么赛制)
7. [第五层：`strategy.py` —— 真正的"大脑"](#七第五层strategypy--真正的大脑)
8. [第六层：合法性双保险](#八第六层合法性双保险)
9. [完整实例走查（真实数字）](#九完整实例走查真实数字)
10. [打包与部署：为什么只有一个文件](#十打包与部署为什么只有一个文件)
11. [函数速查表](#十一函数速查表)
12. [附录：Python 概念小贴士](#附录python-概念小贴士)

---

## 一、整体地图：谁在跟谁说话

想象一场"舞台剧"：

```
┌──────────────┐   ①发 JSON     ┌─────────────────────────────────────┐
│              │ ─────────────► │  bot.py（门卫：读 stdin、写 stdout） │
│   平台        │                │        │                            │
│ (BotArena /  │                │        ▼                            │
│  BotBattle)  │                │  game_state.py   解析协议→状态对象     │
│              │                │  opponent.py     给对手建档案         │
│              │                │  match_ctx.py    记住赛制进度         │
│              │                │  strategy.py     ★ 决策核心           │
│              │                │        │                            │
│              │   ②返回整数     │        ▼                            │
│              │ ◄───────────── │  {"act":"raise","num":300} → 300    │
└──────────────┘                └─────────────────────────────────────┘
```

关键点：**平台不会告诉你"现在该怎么办"**，它只给"当前局面"；也不接受"raise"这种文字，只要一个整数。

### 角色表

| 模块 | 文件 | 一句话职责 | 比喻 |
|---|---|---|---|
| 交互层 | `bot.py` | 读一行 JSON、写一行 JSON | 邮差 / 门卫 |
| 协议层 | `game_state.py` | JSON → `GameState` 对象 | 翻译官 |
| 建模层 | `opponent.py` | 统计对手习惯 | 情报员 |
| 赛制层 | `match_ctx.py` | 记录第几局、领先多少 | 记分员 |
| 决策层 | `strategy.py` | 算牌力、选动作 | 大脑 |
| 打包 | `bundle.py` | 把上面 5 个合成 1 个文件 | 装订工 |

---

## 二、一轮交互的完整时序（10 步）

平台每**轮到我行动**时，会发来一条消息。整个过程走这 10 步：

```
① 平台 → stdin：一行 JSON（含 requests / data / globaldata）
      ↓
② bot.run() 逐行读（for raw in sys.stdin）
      ↓
③ bot._handle_line()：json.loads 解析成 Python 字典
      ↓
④ bot._extract_request()：从 requests 数组里取出"本轮的那一个"
      ↓
⑤ game_state.parse_request()：解析成 GameState 对象
   （同时算出 to_call / 底池 / 阶段 / 是否有人全押…）
      ↓
⑥ opponent.build_model_from_history()：用 history 更新对手统计
      ↓
⑦ match_ctx：从存回来的 globaldata 恢复进度 → ctx.update() 结算上一手
      ↓
⑧ strategy.decide()：★ 真正做决策，返回 {"act": "raise", "num": 300}
      ↓
⑨ bot._to_response() → 300；再经 _final_guard() 做整数级合法性校验
      ↓
⑩ bot 写 stdout：{"response": 300, "data": "...", "globaldata": "..."}
```

**为什么要有 globaldata？** 因为机器人是"无状态"的——平台每回合可能重启进程。
所以机器人把"记忆"（对手统计 + 赛制进度）序列化成字符串，**每次回传**给平台；
下一回合平台再把它塞回来。这就像打牌时把笔记托管给裁判：

```
本轮返回：{"response": 300, "data": "<对手统计JSON>", "globaldata": "<同上>"}
                                                        ↑ 下次平台会还给我
```

---

## 三、第一层：`bot.py` —— 收发消息的门卫

### 3.1 主循环 `run()`

```python
def run():
    for raw in sys.stdin:          # 逐行读；平台发一行，我回一行
        raw = raw.strip()
        if not raw:
            continue
        try:
            obj = json.loads(raw)  # 字符串 → Python 字典
        except Exception:
            continue               # 不是 JSON（比如预热握手）→ 忽略，别崩
        resp, data_out, json_mode = _handle_line(obj)
        if json_mode:
            out = {"response": resp, "data": data_out, "globaldata": data_out}
            sys.stdout.write(json.dumps(out) + "\n")
        else:
            sys.stdout.write(str(resp) + "\n")
        sys.stdout.flush()         # ★ 立刻推出去，不能等缓冲
```

**逐行读而不是一次读完**：如果平台发完不关 stdin（常驻模式），
`sys.stdin.read()` 会一直等到 EOF —— 那就永远不回复了。
`for raw in sys.stdin` 每收到一行就能处理。这个细节曾导致"Bot 未响应"被预检判负。

> **Python 小贴士**
> - `sys.stdin` 是"标准输入"，`sys.stdout` 是"标准输出"，都是管道。
> - `for line in sys.stdin` 会一行一行地取，文件结束后自然停止。
> - `sys.stdout.flush()` = "别攒着，现在就把数据推出去"。不加这句，
>   数据可能留在缓冲区里、程序还没结束 → 平台等超时。

### 3.2 取本轮 request：`_extract_request()`

平台发来的结构大致是：

```json
{
  "requests":  [ {...第1轮...}, {...第2轮...}, {...本轮...} ],
  "responses": [ ...我之前回复的整数... ],
  "data":       "上一轮我存下的字符串",
  "globaldata": "同上"
}
```

```python
def _extract_request(obj):
    reqs = obj.get("requests") or []
    for item in reversed(reqs):        # 从最后一个往前找
        cand = item
        if isinstance(cand, str):      # 有的是字符串，需要再 json.loads 一次
            cand = json.loads(cand)
        if isinstance(cand, dict) and ("my_cards" in cand or "history" in cand):
            return cand                # 找到"看起来像牌局请求"的那个就返回
    return None
```

> **Python 小贴士**
> - `obj.get("requests")` = 安全取值；键不存在返回 `None`，不会像 `obj["requests"]` 那样报错。
> - `reversed(list)` 倒着遍历 → 第一个就是最新的一轮。

### 3.3 核心调度：`_handle_line()`

```python
def _handle_line(obj):
    json_mode = False
    data_str = gdata_str = ""
    request = None

    if isinstance(obj, dict) and "requests" in obj:
        json_mode = True                                  # 平台的包装格式
        data_str  = _persisted_str(obj.get("data"))
        gdata_str = _persisted_str(obj.get("globaldata"))
        request   = _extract_request(obj)
    elif isinstance(obj, dict) and ("my_cards" in obj or "history" in obj):
        request = obj                                     # 本地调试：裸 request

    resp = 0
    data_out = ""
    try:
        state = parse_request(request)                    # ⑤ 解析
        try:
            model = OpponentModel.from_json(gdata_str or data_str)   # ⑦ 恢复记忆
            build_model_from_history(model, request, state)          # ⑥ 更新统计
            ctx = MatchContext.from_dict(model.ctx_dict)             # ⑦ 赛制上下文
            ctx.update(state)                                        #   结算上一手
            ctx.sync_baseline(state)
            action = decide(state, model, ctx, debug=_dbg)           # ⑧ 决策
            resp = _to_response(action)                              # ⑨ 转整数
            resp = _final_guard(state, resp)                         #   合法性兜底
            model.ctx_dict = ctx.to_dict()                           #   存回记忆
            data_out = model.to_json()
        except Exception:
            resp = _fallback_resp(state)      # ★ 任何异常都不能传到平台
    except Exception:
        resp = 0                              # 连解析都失败 → 输出最保守的 0
    return resp, data_out, json_mode
```

**这层的设计哲学：无论内部出什么错，都保证输出一个合法整数。**
因为"返回非法动作"的代价是直接判负，比"这一手打错"严重得多。

### 3.4 动作 → 整数：`_to_response()`

```python
_ACT_TO_RESPONSE = {"fold": -1, "allin": -2, "call": 0, "check": 0}

def _to_response(action):
    act = action.get("act", "fold")
    if act == "raise":
        num = int(action.get("num", 0))
        return num if num > 0 else -1      # raise 的数字就是"本轮我的总注额"
    return _ACT_TO_RESPONSE.get(act, -1)   # 其余查表
```

| 内部动作 | 含义 | 返回整数 |
|---|---|---|
| `{"act": "fold"}` | 弃牌 | `-1` |
| `{"act": "allin"}` | 全押 | `-2` |
| `{"act": "check"}` / `{"act": "call"}` | 过牌 / 跟注 | `0` |
| `{"act": "raise", "num": 300}` | 加注到本轮总注额 300 | `300` |

### 3.5 整数级安全网：`_final_guard()`

即使 `strategy` 说"fold"，这一层也要再核一遍规则：

```python
def _final_guard(state, resp):
    to_call = state.to_call
    my_left = state.my_left

    if resp == -1:                       # 弃牌
        return -1 if to_call > 0 else 0  # ★ 无需跟注时不能弃牌，只能过牌
    if resp == -2:                       # 全押
        return -2 if my_left > 0 else (-1 if to_call > 0 else 0)
    if state.any_allin:                  # 有人全押 → 只能弃牌/全押
        return -2 if my_left > 0 else -1
    if resp == 0:                        # 跟注/过牌
        if to_call <= 0:      return 0   # 过牌合法
        if my_left > to_call: return 0   # 跟注合法（必须严格大于）
        return -2                        # 筹码不够跟 → 只能全押
    # resp > 0：加注，夹到 [min_raise, max_raise) 区间
    num = int(resp)
    num = max(num, state.min_raise())
    if num >= state.max_raise() or num >= my_left:
        return -2
    return num
```

> 注意第一行的 `to_call > 0`：**"弃牌永远合法"是错的**。
> 当不需要跟注（`to_call == 0`）时你只能过牌，返回 -1 会被平台拒收，
> 甚至被 fallback 成别的动作。这是后来修掉的一个真实 bug。

---

## 四、第二层：`game_state.py` —— 把 JSON 翻译成"牌局状态"

这是**最值得细读**的一层：所有"能不能这么打"的判断都源于这里。

### 4.1 request 字段表

| 字段 | 含义 |
|---|---|
| `num_players` | 人数（本项目恒为 2，即单挑 HU）|
| `dealer_id` | 庄家座位号（HU 里庄家=小盲，翻前先行动）|
| `my_id` | 我的座位号 |
| `my_chips` | **我当前剩余筹码**（已经扣掉盲注）|
| `my_cards` | 我的两张底牌 |
| `public_cards` | 公共牌（0/3/4/5 张）|
| `history` | 本手牌至今所有动作 |
| `hand` | 当前第几手（**0-based**：0 表示第 1 手）|
| `max_hand` | 总共多少手（70）|
| `total_win_chips` | 累计净盈亏 `[我, 对手]`（定胜负的依据）|
| `total_win_games` | 累计赢的局数 |

**牌的编码**：0~51 的整数。点数 = `n//4 + 2`，花色 = `n%4`。
程序内部统一 `+8` 转换一次（`self.my_cards = [int(c) + 8 for c in ...]`），
方便后面评估牌型。

> **Python 小贴士**
> - `n // 4` 是"整除"（只要商），`n % 4` 是"取余"。
> - `[x + 8 for x in list]` 叫**列表推导式**，等价于"遍历每个元素做 x+8，收集成新列表"。

### 4.2 座位号归一化（一个真实踩坑）

```python
self.id_base = 1 if (_raw_mid >= 2 or _raw_did >= 2) else 0
if self.id_base == 1:
    _raw_mid -= 1
    _raw_did -= 1
self.my_id = 0 if _raw_mid <= 0 else 1
self.opp_id = 1 - self.my_id          # 单挑：对手就是另一个人
```

有些赛季传"座位 1 / 座位 2"（1-based）。如果不归一化，
`my_id=2` 时 `opp_id = 1-2 = -1`，而 Python 里 `list[-1]` 表示**最后一个元素**，
于是 `total_win_chips[-1]` 会静默取错值 → 锁赢判断全反。

> **Python 小贴士**：负索引 `lst[-1]` = 倒数第一个。这个"方便"的特性
> 在没有范围检查时非常危险。

### 4.3 状态重建 `_analyze()`

这一步的产物（后面策略全靠它们）：

| 属性 | 含义 | 怎么算出来的 |
|---|---|---|
| `current_round` | 0/1/2/3 = 翻前/翻牌/转牌/河牌 | 由公共牌张数决定 |
| `stage` | 上者的文字版 `"preflop"` 等 | 查表 |
| `any_allin` | 本手是否有人全押 | 扫 history 有没有 `allin` |
| `my_round_bet` | 我**本轮**已下注 | 翻前=已投入；翻后=重放本轮 history |
| `opp_round_bet` | 对手本轮已下注 | 同上 |
| `to_call` | **还需跟多少才能继续** | `opp_round_bet - my_round_bet`（不为负）|
| `pot` | 底池 | 双方累计投入之和 |
| `min_raise()` | 最小加注数 | 本轮无人下注→大盲；否则→2×本轮最大注 |
| `max_raise()` | 最大加注数 | `my_chips - to_call - 1` |

**`to_call` 是整个程序里最常被用到的数字**：
- `to_call == 0` → 我可以 **check（过牌）**，不能 fold
- `to_call > 0` → 我必须 **call / raise / fold**

翻前为什么能精确算？看这段：

```python
if not i_acted and my_total_in > 0:      # 我还没行动过，且已投入筹码（=盲注）
    if self.my_id == self.dealer_id:     # 我是庄家/小盲
        self.small_blind = max(my_total_in, 50)
        self.big_blind   = max(2 * my_total_in, 100)
    else:                                # 我是大盲
        self.big_blind   = max(my_total_in, 100)
        self.small_blind = max(my_total_in // 2, 50)
```

思路：我还没主动行动过，但我手里少了筹码 —— 那少掉的**就是盲注**。
反推即可。`max(...)` 是防止把盲注推导成异常小值（曾导致锁赢阈值漂移）。

> **Python 小贴士**
> - `@property` 装饰器：让你像读变量一样调用方法，`state.pot` 而不是 `state.pot()`。
> - `try / except`：捕获异常。本项目大量使用它做"防御性编程"——
>   宁可某个功能降级，也不能让整个程序崩溃。

---

## 五、第三层：`opponent.py` —— 给对手建档案

### 5.1 每次行动都记一笔

```python
def update(self, kind, is_preflop, big=False, street=None):
    # kind: "raise" / "bet" / "allin" / "call" / "fold" / "check"
```

累计统计出这些指标：

| 指标 | 含义 | 用处 |
|---|---|---|
| `vpip` | 入池率（多爱玩牌）| 判断他手牌范围宽窄 |
| `pfr` | 翻前加注率 | 判断能否偷盲 |
| `fold_to_bet` | 面对下注弃牌率 | 决定要不要诈唬 |
| `bet_freq` | 主动下注频率 | 判断凶/被动 |
| `check_fold` | **他过牌后**面对我下注的弃牌率 | 决定"他过我，我要不要下注" |
| `allin_count` | 全押次数 | 判断"爱梭哈"程度 |

### 5.2 小样本不能当真：贝叶斯收缩 `_shrunk()`

```python
def _shrunk(self, raw, n, key):
    k = 12
    return (raw * n + _PRIOR[key] * k) / (n + k)
```

- `raw` = 实际观测到的比例，`n` = 样本数
- `_PRIOR[key]` = 先验值（"还没数据时先假设他是普通玩家"）
- 样本少 → 结果偏向先验；样本多 → 偏向实测

> 例：对手 2 次面对下注全弃牌（raw = 1.0），`n=2`，
> 先验 0.40 → `(1.0×2 + 0.40×12) / (2+12) = 0.49`。
> 不会天真地认为"他 100% 弃牌"。

### 5.3 风格分类 `archetype()`

```python
loose = vpip > 0.60          # 松？（爱入池）
aggro = ef_bet_freq > 0.42   # 凶？（爱下注）

松 + 极凶 → maniac   疯子
松 + 被动 → station  跟注站
紧 + 被动 → rock     岩石
紧 + 凶   → tag      紧凶
```

策略层根据这个标签微调（比如对岩石加注要更尊重、对疯子可以放宽跟注）。

### 5.4 `build_model_from_history()` —— 增量更新

平台每回合都会把**本手至今完整 history** 重发一遍。如果不做去重，
同一个动作会被统计无数次。所以：

```python
model.resp_done[hand] = sorted(done_list)   # 记录"这一手已处理到第几条"
```

下次只处理**新增的**部分。这是"重放式协议"里的标准做法。

---

## 六、第四层：`match_ctx.py` —— 记住"这是第几局、什么赛制"

70 手赛制、按累计净盈亏定胜负 —— 所以"现在领先多少"和"还剩几手"
会彻底改变策略（领先就该收手、落后就该拼）。

```python
ctx.update(state)          # 用本轮的筹码变化结算"上一手"发生了什么
ctx.threshold_offset(state)  # 返回状态机阈值偏移（激进=-6 / 保守=+6，单位大盲）
ctx.strategy_shift()         # 策略学习带来的微调
```

`_match_adjust(state)` 把这些偏移叠加到"领先差"上，最后返回一个档位：

```
normal      正常
protect     领先到可以保守
pressure    落后需要施压
doomed      数学上追不回来了（只能 all-in 搏）
```

---

## 七、第五层：`strategy.py` —— 真正的"大脑"

### 7.1 对外只有一个入口：`decide()`

```python
def decide(state, model, ctx=None, debug=False):
    # 1) 打开日志开关（写 stderr，不污染 stdout 协议）
    DecisionLogger.enable(...)

    # 2) 刷新全部"全局变量"（本手的状态）
    _prepare_globals(state, model, ctx)

    # 3) 【硬规则】锁胜 / 防锁赢 —— 优先级最高，先判，判中直接返回
    _lw0 = _lock_win_unified(state)
    if _lw0 is not None:
        return _normalize(state, _lock_win_legal(state, _lw0))

    # 4) 常规决策（翻前 / 翻后）
    return _decide_impl(state, model, ctx, debug=debug, _lw=_lw0, _prepared=True)
```

> **Python 小贴士：为什么有 `_prepare_globals`？**
> 模块级变量（`_CTX`、`_LEAD_LOCK`…）是"全局的"，下一次调用会被覆盖。
> 如果入口先判断、而这些变量还没刷新，读到的就是**上一手的旧值**。
> 把"刷新全局"抽成一个函数、入口和内层都调它，就不会不一致。

### 7.2 优先级链（决定了"先想什么、后想什么"）

```
① _lock_win_unified     锁胜弃牌 / 防锁赢 all-in      ← 最高，判中就直接返回
② should_avoid_risk     公对弱两对 → 走保守路线
③ _preflop_decide       翻前决策
   _postflop_decide     翻后决策
④ _lock_win_tail_guard  全下下限 + 禁止弃牌兜底
⑤ _bet_cap_guard        注额上限（<三条 不超 3000）
⑥ _aggressive_strong_bet 好牌主动打到 ≥2000
⑦ _normalize            合法性最终校验
```

### 7.3 锁胜/防锁赢：全程序最硬的规则

```python
def _lock_win_unified(state):
    # A. 防锁赢：弃牌后数学上追不回 → 只能 all-in 搏
    if _match_adjust(state) == "doomed":
        return {"act": "allin"}

    # B. 锁胜：领先到"全程弃牌也稳赢" → 弃牌保胜
    if _fold_out_active(state):
        if state.to_call <= 0:
            return {"act": "check"}     # 无需跟注 → 只能过牌
        return {"act": "fold"}

    # C. 盈利锁胜：已投太多，输掉就把胜局送人 → all-in
    if not _is_lead_lock(state) and _profit_lock_allin(state):
        return {"act": "allin"}
    return None
```

判断"锁赢"的核心公式（`_lock_line`）：

```python
锁赢线 = 2 ×（本手已投入 + 本手之后剩余手数 × 平均盲注损失）
领先差 = total_win_chips[我] - total_win_chips[对手]
领先差 > 锁赢线  →  锁定胜局
```

**为什么有 2 倍？** 我弃牌输 X，对手就赢 X —— 一来一回，**差距变化 2X**。

```python
def _hands_left(state):
    """本手之后还剩几手。平台 hand 是 0-based（0..max_hand-1）"""
    h, m = int(state.hand_num), int(state.max_hand)
    if m <= 0 or h >= m:
        return 0
    return max(0, m - 1 - h)
```

> 这个 `-1` 是踩过大坑的：早先写成 `m - h`（把"本手"也算进去），
> 剩余手数多算一手 → 锁赢线虚高约 300 → **该弃牌时不敢弃，反而继续加注**。

### 7.4 翻后决策的输入

`_postflop_decide()` 会先算这些：

```python
eq      = 蒙特卡洛胜率       # 随机模拟对手手牌，跑 N 次算赢的比例
category = _effective_category(state)  # 牌型：高牌0 / 一对1 / 两对2 / 三条3 / 顺子4 / 同花5 ...
tex      = 牌面纹理（湿/干、有没有同花/顺子的可能）
```

> **蒙特卡洛胜率**：不知道对手底牌，就"随机发"很多次，
> 看我的牌赢多少次。跑 500 次，赢 310 次 → 胜率 ≈ 0.62。

然后按牌力分支处理（strong / good / medium / draw / air），
每个分支内部再按位置、底池大小、对手类型选尺寸。

### 7.5 日志：看得见的决策过程

```python
DecisionLogger.log(hand, street, pot, rule, action, detail)
```

写的是 **stderr** 而不是 stdout —— 因为 **stdout 是平台的协议通道**，
往里面多写一行日志，平台就会把日志当成"回复" → 解析失败 → 判负。

---

## 八、第六层：合法性双保险

程序里有**两道**合法性检查，位置不同、粒度不同：

| 层 | 位置 | 粒度 | 作用 |
|---|---|---|---|
| 第一道 | `strategy._normalize()` | 动作字典 | 把非法动作**改写成**合法的（如 check→fold、raise 越界→allin）|
| 第二道 | `bot._final_guard()` | 整数 | 最后的整数校验，保证发出去的一定合法 |

`_normalize()` 的几条主干规则：

```python
if state.any_allin:               # 有人全押
    → 只允许 fold / allin
if act == "fold":
    → 若 doomed（弃牌=送胜局）则升级为 allin
if act == "check":
    → 若 to_call > 0 则不能过牌，改成 fold
if act == "call":
    → 若筹码不够跟 → 改成 allin
if act == "raise":
    → 夹到 [min_raise, max_raise)，越界改 allin
```

**设计理念**：决策层可以"想得很激进"，但输出层一定"很保守"——
这样即使策略里有 bug，也不会因为非法动作判负。

---

## 九、完整实例走查（真实数字）

假设平台发来这一轮：

```json
{
  "requests": [{
    "num_players": 2, "dealer_id": 1, "my_id": 0,
    "my_chips": 19900, "my_cards": [4, 20],
    "public_cards": [], "history": [],
    "hand": 46, "max_hand": 70,
    "total_win_chips": [1816, -1816], "total_win_games": [0, 0]
  }],
  "data": "", "globaldata": ""
}
```

### 逐层处理

**第 4 步 · `_extract_request`** → 取出上面那个 dict

**第 5 步 · `parse_request`**

```
my_cards  [4,20] → 内部编码 [12,28]
my_id=0, dealer_id=1 → 我不是庄家 → 我是大盲
my_chips=19900 → 已投入 100（= 大盲）
public_cards 空 → current_round = 0 → stage = "preflop"
to_call = 100(大盲) - 100(我已投) = 0
```

**第 6–7 步 · 建模 + 赛制**

```
对手统计：vpip≈0.42, fold_to_bet≈0.68 → archetype = "rock"（岩石）
ctx：第 47 手（hand=46 是 0-based），剩 70-1-46 = 23 手
```

**第 8 步 · `decide()`**

```
① _lock_win_unified:
     领先差 = 1816 - (-1816) = 3632
     锁赢线 = 2 × (盲注线(23手) + 本手已投 100)
            = 2 × (1700 + 100) = 3600
     3632 > 3600 → ✅ 已锁赢！
     to_call = 0 → 不能弃牌 → 返回 {"act": "check"}

   决策结束（不再往下走）
```

**第 9 步 · 转整数**

```
_to_response({"act":"check"}) → 0
_final_guard(state, 0)        → to_call=0 → 0 合法 → 0
```

**第 10 步 · 输出**

```json
{"response": 0, "data": "{...对手统计...}", "globaldata": "{...}"}
```

`0` = 过牌（不用投入筹码）→ 稳稳保住领先优势。**这一手就算走完了。**

---

## 十、打包与部署：为什么只有一个文件

平台要求提交**单个可执行文件**，但项目有 9 个模块。`bundle.py` 负责合并：

```python
ORDER = ["cards", "evaluator", "ranges", "game_state",
         "equity", "opponent", "match_ctx", "strategy", "bot"]

def bundle():
    for m in ORDER:                     # 按依赖顺序拼接（被依赖者在前面）
        src = open(m + ".py").read()
        parts.append(_strip_module(src))   # 去掉内部 import 和 if __name__ 块
    parts.append("if __name__ == '__main__':\n    run()")
```

**为什么要去掉内部 import？** 合并后所有函数都在同一个命名空间里，
`from game_state import parse_request` 反而会找不到模块而报错。

### 完整发布流水线

```
① 改源码（内容/*.py）
② python bundle.py                 → 生成 内容/botzone_submit.py（单文件）
③ cp 到 botbattle/poker_bot.py      → 推送到 GitHub
④ GitHub Actions 自动：
     · 把 git 短 SHA 写进 BOT_VERSION（版本可自证）
     · PyInstaller --onefile 打成 Linux ELF
     · 产物名 poker_bot-linux-x86-64-<短SHA>
⑤ 下载 ELF → 上传平台
```

---

## 十一、函数速查表

| 文件 | 函数 | 输入 | 输出 | 说明 |
|---|---|---|---|---|
| bot.py | `run()` | — | — | 主循环，逐行收发 |
| bot.py | `_handle_line(obj)` | 平台 JSON | `(resp, data, json_mode)` | 单轮调度 |
| bot.py | `_extract_request(obj)` | 平台 JSON | request dict | 取本轮请求 |
| bot.py | `_to_response(action)` | `{"act":...}` | 整数 | 动作→整数 |
| bot.py | `_final_guard(state, resp)` | 状态+整数 | 合法整数 | 第二道安全网 |
| bot.py | `_fallback_resp(state)` | 状态 | 整数 | 异常时的保守回复 |
| game_state | `parse_request(request)` | request dict | `GameState` | 协议解析入口 |
| game_state | `GameState._analyze()` | — | — | 算出 to_call/pot/stage 等 |
| game_state | `min_raise()/max_raise()` | — | 整数 | 加注边界 |
| opponent | `build_model_from_history(m, req, st)` | 模型+req+状态 | — | 增量更新对手统计 |
| opponent | `archetype()` | — | 字符串 | 风格分类 |
| match_ctx | `ctx.update(state)` | 状态 | — | 结算上一手 |
| match_ctx | `ctx.threshold_offset(state)` | 状态 | 整数(BB) | 阈值偏移 |
| strategy | `decide(state, model, ctx)` | 三件套 | `{"act":...}` | ★ 决策总入口 |
| strategy | `_lock_win_unified(state)` | 状态 | action 或 None | 锁胜/防锁赢 |
| strategy | `_hands_left(state)` | 状态 | 整数 | 本手之后剩几手 |
| strategy | `_lock_line(state)` | 状态 | 整数 | 锁赢线 |
| strategy | `_normalize(state, action)` | 状态+动作 | 合法动作 | 第一道安全网 |
| strategy | `explain(state, model)` | 状态+模型 | dict | 日志用快照（不改决策）|

---

## 附录：Python 概念小贴士

<details>
<summary><b>字典 dict：用"键"取值</b></summary>

```python
action = {"act": "raise", "num": 300}
action["act"]        # "raise"  键不存在会报错 KeyError
action.get("num", 0) # 300      键不存在返回默认值 0（更安全）
```
本项目里 `request`、`action` 都是 dict。

</details>

<details>
<summary><b>列表 list 与切片</b></summary>

```python
xs = [10, 20, 30]
xs[0]      # 10    从 0 开始数
xs[-1]     # 30    负数 = 从后面数（危险！）
xs[1:]     # [20, 30]  切片：从下标 1 到末尾
```

</details>

<details>
<summary><b>推导式：一行写完"遍历 + 计算"</b></summary>

```python
# 传统写法
out = []
for c in my_cards:
    out.append(c + 8)

# 推导式
out = [c + 8 for c in my_cards]
```

</details>

<details>
<summary><b>try / except：出错也不崩</b></summary>

```python
try:
    num = int(text)          # 可能失败
except Exception:
    num = 0                  # 失败了就走这里
```
本项目几乎每个函数都包了 try/except —— 因为**平台只看结果**，
崩一次就可能直接判负。

</details>

<details>
<summary><b>函数 = 输入 → 处理 → 返回</b></summary>

```python
def add(a, b):      # a、b 是"参数"（输入）
    return a + b    # return 把结果交出去（输出）

result = add(1, 2)  # result = 3
```
没有 `return` 的函数返回 `None`。

</details>

<details>
<summary><b>@property：伪装成变量的方法</b></summary>

```python
class GameState:
    @property
    def pot(self):
        return (20000 - self.my_chips) + (20000 - self.opp_chips)

st.pot      # 像读变量一样，实际是在调用方法
```
好处：调用方不用关心"这是存的还是算的"。

</details>

<details>
<summary><b>global：在函数里修改模块级变量</b></summary>

```python
_COUNT = 0

def bump():
    global _COUNT    # 不加这句，函数内只是新建一个局部变量
    _COUNT += 1
```
本项目用 `global` 传递"本手的状态"（`_CTX`、`_LEAD_LOCK`…），
也正因为如此，**入口处必须先把它们刷新一次**，否则会读到上一手的旧值。

</details>

<details>
<summary><b>类与实例：模板 vs 实物</b></summary>

```python
class Dog:                 # 类 = 模板
    def __init__(self, name):
        self.name = name   # self = 这个"实物"自己
    def bark(self):
        return self.name + " 汪！"

d = Dog("旺财")            # 实例 = 按模板造出来的实物
d.bark()                   # "旺财 汪！"
```
本项目里：`OpponentModel()`（对手档案）、`GameState(request)`（一手的局面）
都是"类"，每轮都会新建/恢复一个实例。

</details>

---

## 一句话总结整个流程

> **平台给"局面"（JSON）→ `bot.py` 取出来 → `game_state` 翻译成对象
> → `opponent`/`match_ctx` 补上记忆 → `strategy.decide` 按优先级链算出动作
> → 两道合法性校验 → `bot.py` 把动作变成整数写回平台。**

看代码时建议从这个顺序读：
`bot.py: run() → _handle_line()` → `game_state.py: GameState` →
`strategy.py: decide()`（其余模块按需展开）。
