# 德州扑克机器人 —— 代码逐行详解

> 配套文档：《代码流程说明.md》（讲整体流程）；本篇**逐行拆解关键代码**，
> 说明每一行在做什么、调用了哪个函数、为什么这样写（含踩坑历史）。
>
> 源码位置：`内容/*.py`；文中代码与清理后的版本一致（bundle 4857 行）。

---

## 阅读方式

每个函数按下面四段展开：

1. **作用** —— 一句话
2. **代码逐行** —— 代码块 + 行内注释
3. **关键变量表** —— 每个变量是什么
4. **为什么这么写** —— 反直觉处的历史原因

建议按 `bot.py → game_state.py → opponent.py → strategy.py` 顺序读。

---

# 第一部分：`bot.py`（平台交互层，237 行）

## 1.1 全局常量与工具

```python
_ACT_TO_RESPONSE = {"fold": -1, "allin": -2, "call": 0, "check": 0}
```
**逐行解释**：一个**字典**，把内部动作用的名字映射成平台要求的整数。
- `"fold"`（弃牌）→ `-1`
- `"allin"`（全押）→ `-2`
- `"call"`（跟注）/ `"check"`（过牌）→ 都是 `0`（平台用同一个 0 表示"不加注"）
- 加注（`"raise"`）不在表里，因为它的数字是**变量**（加注后的总注额）。

```python
_LAST_HAND = [None]        # 用单元素列表当"可变全局"（函数里改得动）
```
**为什么用列表而不是 `_LAST_HAND = None`？**
因为函数内要修改它。若写成普通变量，函数里赋值会变成"新建局部变量"，
必须写 `global`。用列表 `lst[0] = x` 是"改内容"而非"重新绑定"，不需要 `global`。
这是一个常见的小技巧。

```python
def _log(msg):
    """诊断输出 —— 必须写 stderr（stdout 是平台协议通道）。"""
    try:
        import sys as _sys
        _sys.stderr.write(msg + "\n")
        _sys.stderr.flush()
    except Exception:
        pass
```
**逐行**：
- `import sys as _sys`：函数内导入（局部导入），避免模块级污染。
- `_sys.stderr.write(...)`：写**标准错误**。**关键**：`stdout` 是平台的协议通道，
  往里写日志会被平台当成"回复"→ 解析失败 → 判负。
- `.flush()`：立刻推出去，别留在缓冲区。
- `try/except`：日志失败也绝不能影响主流程。

## 1.2 动作 → 整数：`_to_response()`

```python
def _to_response(action):
    """把内部动作 dict 转为 response 整数。"""
    act = action.get("act", "fold")          # ① 取出动作名；缺省当弃牌（最保守）
    if act == "raise":                       # ② 加注要单独处理
        try:
            num = int(action.get("num", 0))  # ③ 取加注后的"本轮总注额"
        except (TypeError, ValueError):
            return -1                        # ④ 取不出来 → 弃牌（保守）
        return num if num > 0 else -1        # ⑤ 非正数无意义 → 也弃牌
    return _ACT_TO_RESPONSE.get(act, -1)     # ⑥ 其余动作查表；未知动作 → 弃牌
```
**关键变量**
| 变量 | 含义 |
|---|---|
| `action` | 决策层返回的字典，如 `{"act": "raise", "num": 300}` |
| `num` | **raise-to 语义**：不是"加多少"，而是"加完后我这轮总共下注多少" |

**为什么这么写**：`get(act, -1)` 的兜底很关键——
万一决策层返回了个 `{"act": "blah"}`，这里会退化成弃牌，而不是让平台收到非法值。

## 1.3 第二道安全网：`_final_guard()`

```python
def _final_guard(state, resp):
    """整数 response 层面的最后一道安全网，绝对保证合法。"""
    try:
        to_call = state.to_call      # ① 我还需要跟多少（0 = 可以过牌）
        my_left = state.my_left      # ② 我还剩多少筹码

        if resp == -1:               # ③ 弃牌
            return -1 if to_call > 0 else 0
```
**③ 解释（重要）**：原始写法是"弃牌永远合法"，**这是错的**。
当 `to_call == 0`（对方没下注）时，你**不能弃牌**——只能过牌。
返回 `-1` 会被平台拒收，甚至 fallback 成别的动作（可能是加注）。
所以：需要跟注才能弃；不需要跟注就必须改成 `0`（过牌）。

```python
        if resp == -2:                       # ④ 全押
            if my_left > 0:
                return -2                    #    还有筹码 → 全押合法
            return -1 if to_call > 0 else 0  #    没筹码了 → 退化成弃/过

        # 规则5：本局有人全押 → 只能弃牌(-1)/全押(-2)
        if state.any_allin:
            return -2 if my_left > 0 else -1
```
**④/规则5 解释**：本手只要出现过 all-in，平台就只接受两种动作。
这是规则层面的硬约束，所以放在这里"一刀切"。

```python
        if resp == 0:                        # ⑤ 跟注 / 过牌
            if to_call <= 0:
                return 0                     #    无需跟注 → 过牌合法
            if my_left > to_call:
                return 0                     #    筹码够 → 跟注合法
            return -2                        #    筹码不够完整跟 → 只能全押
```
**⑤ 注意 `> to_call` 而不是 `>=`**：平台要求"跟注后剩余筹码必须严格大于 0"，
即 `my_left - to_call > 0`。等于时会判非法。

```python
        num = int(resp)                      # ⑥ 加注
        if num <= 0:
            return -1
        min_r = state.min_raise()            #    最小加注（本轮最大注的 2 倍）
        max_r = state.max_raise()            #    最大加注（留出跟注空间）
        if num < min_r:
            num = min_r                      #    太小 → 抬到最小
        if num >= max_r or num >= my_left:
            return -2                        #    太大 → 直接全押
        return num
    except Exception:
        return -1                            # ⑦ 任何异常 → 弃牌（最保守）
```
**⑥ 解释**：加注数被"夹"到合法区间。超上限时不报错，而是转成全押——
全押永远是合法的进攻动作，比返回一个越界数字安全。

## 1.4 异常兜底：`_fallback_resp()`

```python
def _fallback_resp(state):
    """决策层异常时的保守动作：能过牌就过牌，能跟就跟，否则弃牌。"""
    try:
        if state.any_allin:
            return -1                      # ① 有人全押 → 不跟，弃牌
        if state.to_call <= 0:
            return 0                       # ② 不用跟 → 过牌（免费看牌）
        if state.my_left > state.to_call:
            return 0                       # ③ 跟得起 → 跟注
        return -1                          # ④ 都做不到 → 弃牌
    except Exception:
        return -1                          # ⑤ 连状态都读不了 → 弃牌
```
**设计理念**：这是"决策层抛异常"时的救生圈。优先级是
**过牌 > 跟注 > 弃牌**——因为过牌/跟注不会立刻输掉这一手，而弃牌会。

## 1.5 取本轮请求：`_extract_request()`

```python
def _extract_request(obj):
    """从 BotZone JSON 交互输入中取出本轮 request dict（可能为 None）。"""
    reqs = obj.get("requests") or []        # ① 取数组；缺失/None → 空列表
    for item in reversed(reqs):             # ② 从最后一个往前遍历（最新在最前）
        cand = item
        if isinstance(cand, str):           # ③ 有时元素是 JSON 字符串，要再解析
            try:
                cand = json.loads(cand)
            except Exception:
                continue                    #    解析不了 → 试下一个
        if isinstance(cand, dict) and ("my_cards" in cand or "history" in cand):
            return cand                     # ④ 找到"看起来像牌局请求"的就返回
    return None                             # ⑤ 都没找到 → None（上层走保守分支）
```
**关键变量**：`reversed(reqs)` 让最新的请求排在最前，取到就返回。

## 1.6 持久化字符串：`_persisted_str()`

```python
def _persisted_str(v):
    if isinstance(v, str):     # ① 已经是字符串 → 直接用
        return v
    if v is None:              # ② None → 空串
        return ""
    try:
        return json.dumps(v)   # ③ 是对象 → 序列化成字符串
    except Exception:
        return ""              # ④ 序列化失败 → 空串（宁可丢记忆，不能崩）
```

## 1.7 单轮调度：`_handle_line()`

```python
def _handle_line(obj):
    """处理一行 JSON 输入，返回 (resp, data_out, json_mode)。"""
    json_mode = False
    data_str = gdata_str = ""
    request = None

    if isinstance(obj, dict) and "requests" in obj:      # ① 平台包装格式
        json_mode = True
        data_str = _persisted_str(obj.get("data"))
        gdata_str = _persisted_str(obj.get("globaldata"))
        request = _extract_request(obj)
    elif isinstance(obj, dict) and ("my_cards" in obj or "history" in obj):
        request = obj                                    # ② 本地调试：裸 request

    resp = 0
    data_out = ""
    try:
        if request is None:
            resp = 0                    # ③ 拿不到状态 → 交过牌（最保守）
        else:
            state = parse_request(request)               # ④ 解析协议 → 状态对象
            try:
                model = OpponentModel.from_json(gdata_str or data_str)   # ⑤ 恢复记忆
                build_model_from_history(model, request, state)          # ⑥ 增量更新
                ctx = MatchContext.from_dict(model.ctx_dict)             # ⑦ 赛制上下文
                ctx.update(state)                                        #    结算上一手
                ctx.sync_baseline(state)                                 #    同步激进档
                ...
                action = decide(state, model, ctx, debug=_dbg)           # ⑧ 决策
                _raw = _to_response(action)                              # ⑨ 转整数
                resp = _final_guard(state, _raw)                          #    合法性校验
                model.ctx_dict = ctx.to_dict()                            #    存回记忆
                data_out = model.to_json()
            except Exception:
                resp = _fallback_resp(state)   # ⑩ 决策链任何异常 → 保守动作
    except Exception:
        resp = 0                               # ⑪ 连解析都失败 → 交过牌
    return resp, data_out, json_mode
```
**逐行要点**
| 行 | 说明 |
|---|---|
| ⑤ | `from_json(gdata_str or data_str)` —— 优先用 globaldata，缺失时退回 data |
| ⑥ | 用本手完整 history 增量更新对手统计（内部有去重游标）|
| ⑦ | 从 `model.ctx_dict` 恢复赛制上下文（第几手、领先多少、激进度）|
| ⑧ | ★ 决策核心，返回 `{"act": ...}` |
| ⑨ | 转成整数 → 过 `_final_guard` |
| ⑩ | **任何**异常都不外抛，退化成保守动作 |

**为什么两层 `try/except`？** 外层管"协议解析"，内层管"决策"。
分开是为了让 `state` 可用——内层异常时还能用 `state` 算出保守动作。

## 1.8 主循环：`run()`

```python
def run():
    for raw in sys.stdin:          # ① 逐行读（不是一次读完！）
        raw = raw.strip()          # ② 去掉首尾空白/换行
        if not raw:
            continue               # ③ 空行跳过
        try:
            obj = json.loads(raw)  # ④ 字符串 → 字典
        except Exception:
            continue               # ⑤ 非 JSON（如预热握手）→ 忽略，不崩
        resp, data_out, json_mode = _handle_line(obj)
        if json_mode:              # ⑥ 平台格式 → 回一个 JSON 对象
            out = {"response": resp, "data": data_out, "globaldata": data_out}
            sys.stdout.write(json.dumps(out) + "\n")
        else:                      # ⑦ 调试格式 → 只回整数
            sys.stdout.write(str(resp) + "\n")
        sys.stdout.flush()         # ⑧ 立刻推出去，否则平台会等到超时
```
**① 为什么不用 `sys.stdin.read()`？**
如果平台发完请求后**不关闭** stdin（常驻模式），`read()` 会一直阻塞等 EOF
→ 机器人 8 秒不回复 → 被判"未响应"（真实踩过的坑）。
逐行读在"每轮重启"和"常驻模式"下都能工作。

**⑧ 为什么必须 `flush()`？**
stdout 默认是"行缓冲/块缓冲"。不 flush 的话数据可能留在缓冲区里，
程序还没退出 → 平台读不到 → 超时。

---

# 第二部分：`game_state.py`（协议解析层）

## 2.1 常量

```python
INIT_CHIPS = 20000        # 每手牌重置的初始筹码
DEFAULT_BIG_BLIND = 100   # 盲注默认（HU 固定 50/100）
```
**为什么要有默认盲注？** 因为平台的 request **不告诉你盲注**。
翻前可以从"我少了多少筹码"反推；翻后（公共牌已发）就没法反推了，
只能用默认值。若这个值错了，只会影响下注尺寸（EV），不会产生非法操作。

## 2.2 构造：`GameState.__init__()`

```python
self.request = request                              # ① 原始 request（调试用）
self.num_players = request.get("num_players", 2)    # ② 人数（HU 恒为 2）
```

### 座位号归一化

```python
try:
    _raw_mid = int(request.get("my_id", 0))
except Exception:
    _raw_mid = 0
try:
    _raw_did = int(request.get("dealer_id", 0))
except Exception:
    _raw_did = 0
self.id_base = 1 if (_raw_mid >= 2 or _raw_did >= 2) else 0   # ③ 判断基准
if self.id_base == 1:
    _raw_mid -= 1                                            # ④ 1-based → 0-based
    _raw_did -= 1
self.my_id = 0 if _raw_mid <= 0 else (1 if _raw_mid >= 1 else 0)
self.dealer_id = 0 if _raw_did <= 0 else (1 if _raw_did >= 1 else 0)
self.opp_id = 1 - self.my_id                                 # ⑤ 单挑：对手是另一个
```
**为什么需要 ③④？**
有的赛季传"座位 1 / 座位 2"。若不归一化，`my_id = 2` 时
`opp_id = 1 - 2 = -1` —— 而 Python 的 `list[-1]` 表示**最后一个元素**，
于是 `total_win_chips[-1]` 会**静默取错值**（不报错！）→ 锁赢判断全反。
这是一个非常隐蔽的 bug，所以必须显式归一化。

### 牌面编码

```python
self.my_cards = [int(c) + 8 for c in request.get("my_cards", [])]
self.public_cards = [int(c) + 8 for c in request.get("public_cards", [])]
```
**+8 是干什么的？** 平台牌号是 0~51（`点数索引*4 + 花色`）。
内部统一用 `点数*4 + 花色`（点数 2~14）→ 正好差 8。
例：平台 `48`（A♠）→ 内部 `56` = 14*4 + 0。

### 长度兜底

```python
_twc = request.get("total_win_chips", [0, 0]) or [0, 0]
try:
    _twc = list(_twc)
except Exception:
    _twc = [0, 0]
while len(_twc) < 2:
    _twc.append(0)               # 不足 2 个就补 0
self.total_win_chips = _twc
```
**为什么？** 后面要用 `total_win_chips[my_id]` 取值。若平台只给了 1 个元素，
会抛 `IndexError` → 被上层 `except` 吞掉 → **判定静默失效**（不报错但结果错）。
宁可补 0，也不要静默失效。

```python
self.small_blind = DEFAULT_BIG_BLIND // 2   # 50
self.big_blind = DEFAULT_BIG_BLIND          # 100
self._analyze()                             # ⑥ 推导所有派生状态
```

## 2.3 状态重建：`_analyze()`

```python
hist = self.history
n_pub = len(self.public_cards)
self.current_round = 0 if n_pub == 0 else (1 if n_pub <= 3 else (2 if n_pub == 4 else 3))
```
**逐行**：公共牌张数决定街：0 张 = 翻前(0)、3 张 = 翻牌(1)、
4 张 = 转牌(2)、5 张 = 河牌(3)。

```python
self.any_allin = False
for r in hist:
    if r.get("action_type") == "allin" or r.get("action") == -2:
        self.any_allin = True
```
**逐个动作扫 history**：只要有一条是全押，本手后续就只能"弃牌/全押"。

```python
my_total_in = max(0, INIT_CHIPS - self.my_chips)   # 我本手已投入总量
cur = [r for r in hist if int(r.get("round", 0)) == self.current_round]
i_acted = any(r.get("player_id") == self.my_id for r in cur)
```
**逐行**：
- `my_total_in`：本手开始时 20000，现在少了多少就是投了多少。
- `cur`：**列表推导式**，只保留"本轮"（当前街）的动作。
- `i_acted`：本轮我是否已经行动过。

### 翻前：反推盲注

```python
if self.current_round == 0:
    self.my_round_bet = my_total_in            # 我本轮注额 = 已投入
    opp_last = None
    for r in cur:
        if r.get("player_id") == self.opp_id:
            opp_last = r                       # 找出对手最近一次动作

    if not i_acted and my_total_in > 0:        # 我还没行动，但已投了筹码
        if self.my_id == self.dealer_id:       # 我是庄家/小盲
            self.small_blind = max(my_total_in, DEFAULT_BIG_BLIND // 2)
            self.big_blind = max(2 * my_total_in, DEFAULT_BIG_BLIND)
        else:                                  # 我是大盲
            self.big_blind = max(my_total_in, DEFAULT_BIG_BLIND)
            self.small_blind = max(my_total_in // 2, DEFAULT_BIG_BLIND // 2)
```
**核心思路**：我没主动下注，但筹码少了 → 少掉的**就是盲注**。
- 我是小盲：已投 = SB，所以 `BB = 2 × SB`
- 我是大盲：已投 = BB

**`max(...)` 是干嘛的？** 防守。曾经出现平台只扣了 50（本该 100），
反推出 `BB = 50` → 锁赢线减半 → 误触发弃牌。加下界钳制后就不会了。

```python
    if opp_last is None:
        self.opp_round_bet = self.big_blind     # 我先行动 → 对手"隐含"下了大盲
        self._opp_round_known = True
    else:
        a = opp_last.get("action", 0)
        at = opp_last.get("action_type", "")
        if at == "raise" or (isinstance(a, int) and not isinstance(a, bool) and a > 0):
            self.opp_round_bet = int(a)          # raise-to：数字即总注额
            self._opp_round_known = True
        elif at == "allin" or a == -2:
            self.opp_round_bet = self.my_round_bet + self.my_chips  # 上界估计
            self._opp_round_known = False        # 金额未知！
        else:                                     # call / check
            self.opp_round_bet = self.my_round_bet
            self._opp_round_known = True
```
**`_opp_round_known` 是什么？** 标记"我知道对手本轮下了多少"。
对手 all-in 时平台**不给金额**，只能估个上界并标记"未知"，
后面 `to_call` 会走特殊分支。

**`isinstance(a, int) and not isinstance(a, bool)` 为什么这么啰嗦？**
Python 里 `True == 1`，`isinstance(True, int)` 也是 `True`。
如果不排除 bool，某个字段恰好为 `True` 就会被当成"下注 1"。

### 翻后：重放本轮

```python
else:
    rb = [0, 0]                                  # 两人本轮注额
    for r in cur:
        p = int(r.get("player_id", 0))
        if p != 0 and p != 1:
            continue
        a = r.get("action", 0)
        at = r.get("action_type", "")
        if at == "raise" or (isinstance(a, int) and not isinstance(a, bool) and a > 0):
            rb[p] = max(rb[p], int(a))           # raise-to：取最大值
        elif at == "allin" or a == -2:
            rb[p] = max(rb[p], max(rb))          # 金额未知，只估上界
        elif at == "fold" or a == -1:
            pass                                 # 弃牌不加注
        else:                                    # call / check：跟平当前最大值
            rb[p] = max(rb)
    self.my_round_bet = rb[self.my_id]
    self.opp_round_bet = rb[self.opp_id]
    self._opp_round_known = True
```
**逐行**：翻后不涉及盲注，直接把本轮动作重放一遍算出各自注额。
`max(rb)` 表示"跟平到当前最大注"。

### 算 `to_call`

```python
if self._opp_round_known:
    self._to_call = max(0, self.opp_round_bet - self.my_round_bet)
else:
    self._to_call = self.my_chips       # 对手全押金额未知 → 按需全押应对
if self.any_allin:
    self._to_call = max(self._to_call, 1)   # 保证 > 0（不能过牌）
```
**① `max(0, ...)`**：跟注差额不能是负数。
**② 未知时用 `my_chips`**：意思是"要跟就得全下"。
**③ 有人全押时强制 ≥1**：因为规则规定此后不能过牌（`to_call == 0` 会被理解成可过牌）。

### 派生属性（`@property`）

```python
@property
def stage(self):
    return ("preflop", "flop", "turn", "river")[min(self.current_round, 3)]

@property
def to_call(self):
    return self._to_call

@property
def is_button(self):
    """庄家位（heads-up 中庄家=小盲，翻前先行动）。"""
    return self.my_id == self.dealer_id

@property
def pot(self):
    """底池估计 = 双方累计投入之和。"""
    return (INIT_CHIPS - self.my_chips) + (INIT_CHIPS - self.opp_chips)
```
**`@property` 的好处**：调用方写 `state.pot` 就行，不用关心它是存的还是算的；
也不会被误赋值。

### 合法加注边界

```python
def min_raise(self):
    """最小加注数（raise-to 语义）"""
    cur_max = self.my_round_bet
    if self._opp_round_known:
        cur_max = max(cur_max, self.opp_round_bet)
    if cur_max <= 0:
        return self.big_blind        # 本轮无人下注 → 至少下大盲
    return 2 * cur_max               # 否则 → 本轮最大注的 2 倍

def max_raise(self):
    """最大加注数（保守上界）"""
    return max(0, self.my_chips - self._to_call - 1)
```
**为什么 `- 1`？** 平台要求跟注后剩余筹码严格 > 0，所以留 1 的余量最保险。

---

# 第三部分：`opponent.py`（对手建模）

## 3.1 先验值

```python
_PRIOR = {
    "vpip": 0.68, "pfr": 0.30, "af": 0.35, "fold_to_bet": 0.40,
    "big_raise": 0.25, "turn_aggr": 0.35, "river_fold": 0.40,
}
```
**含义**：还没有数据时，**假设**对手是个"普通玩家"。
样本少时结论偏向这些值，样本多时才相信实测。

## 3.2 贝叶斯收缩：`_shrunk()`

```python
def _shrunk(self, raw, n, key):
    k = 12                                        # 先验权重（相当于 12 个样本）
    return (raw * n + _PRIOR[key] * k) / (n + k)
```
**逐行**
- `raw` = 观测到的比例（如"5 次里弃牌 4 次" → 0.8）
- `n` = 样本数（5）
- 公式 = **加权平均**：实测占 `n` 份，先验占 `k = 12` 份

**算个例子**：`raw=0.8, n=5, prior=0.40`
```
(0.8*5 + 0.40*12) / (5+12) = (4 + 4.8) / 17 = 0.518
```
不会天真地认为"他 80% 会弃牌"，而是 0.52 —— 样本少时更保守。

**为什么用 12 做权重？** 这是调参结果：约等于"12 手牌之后才开始相信实测"。
太小会被噪声带偏，太大则反应迟钝。

## 3.3 风格分类：`archetype()`

```python
def archetype(self):
    if self.sample_size() < 6:      # ① 样本 < 6 手 → 不下结论
        return "unknown"
    vpip = self.eff_vpip()          # ② 入池率（越大越"松"）
    af = self.eff_bet_freq()        # ③ 主动下注频率（越大越"凶"）
    loose = vpip > 0.60             # ④ 松？
    aggro = af > 0.42               # ⑤ 凶？
    if loose and af > 0.52:
        return "maniac"             #    松 + 极凶 = 疯子
    if loose and not aggro:
        return "station"            #    松 + 被动 = 跟注站
    if not loose and not aggro:
        return "rock"               #    紧 + 被动 = 岩石
    if not loose and aggro:
        return "tag"                #    紧 + 凶 = 紧凶
    return "station" if vpip > 0.5 else "tag"
```
**二维分类**：一张 2×2 的表格。
| | 凶（af > 0.42）| 被动 |
|---|---|---|
| **松**（vpip > 0.60）| maniac 疯子 | station 跟注站 |
| **紧** | tag 紧凶 | rock 岩石 |

策略层据此调整（对岩石要尊重他的加注；对跟注站少诈唬、多做价值注）。

## 3.4 增量更新：`build_model_from_history()`

平台每回合都重发**本手至今完整 history**。如果不做去重，同一动作会被统计很多次。

```python
hand = request.get("hand", None)
is_new_hand = (hand != model.last_hand)      # ① 是否换手了

if is_new_hand:                              # ② 换手时结算上一手
    model.last_hand = hand
    model.hand_bet_counts.append(model.cur_hand_bets)   # 记录上一手下注次数
    if model.last_opp_count > 0:
        model.hands_seen += 1                # 对手上一手有动作 → 计 1 手样本
        if model.hands_seen % _DECAY_EVERY == 0:
            model.decay(0.5)                 # 每 N 手把所有计数打 5 折（近期优先）
    model.last_opp_count = 0
```
**关键变量**
| 变量 | 含义 |
|---|---|
| `last_hand` | 上次处理的 hand 编号 |
| `hands_seen` | 观测到对手动作的手数（样本量）|
| `decay(0.5)` | 指数衰减：老数据的权重每 N 手减半 |

```python
done_list = set(model.resp_done.get(hand, ()))    # ③ 本手已处理到第几条
for i, r in enumerate(hist):
    if i in done_list:
        continue                                  #    已处理 → 跳过
    if r.get("player_id") != my_id:
        continue                                  #    只看自己的下注
    ...
    model._add_bet_resp(hand, pf, bucket, reaction, after_check=_ack)
    done_list.add(i)                              # ④ 标记已处理
model.resp_done[hand] = sorted(done_list)
```
**③④ 是"游标去重"**：记录"这一手已经处理到第几条 history"，
下次只处理新增部分。这是**重放式协议**（每回合重发全量历史）的标准做法。

---

# 第四部分：`strategy.py` 关键路径

## 4.1 全局状态与哨兵

```python
_CTX = None              # 当前请求的赛制上下文
_MODEL_REF = None        # 当前对手模型
_LEAD_LOCK = False       # 优势锁定标志
_DECISION_STARTED_AT = 0.0   # 决策开始时间（超时保护）
_LW_UNSET = object()     # 哨兵对象
```
**`_LW_UNSET = object()` 是干什么的？**
用来区分两种"没有值"：
- `None` = "已经算过了，结果是不触发"
- `_LW_UNSET` = "还没算过"

因为 `object()` 每次创建都是**独一无二**的对象，可以用 `is` 精确比较。

## 4.2 刷新全局：`_prepare_globals()`

```python
def _prepare_globals(state, model, ctx, reset_clock=True):
    global _CTX, _OPP_JUMPED, _LEAD_LOCK, _OPP_BETS_PER_HAND, \
        _DECISION_STARTED_AT, _MODEL_REF       # ① 声明要改这些模块级变量
    if reset_clock:
        _DECISION_STARTED_AT = time.perf_counter()   # ② 记录决策起点
    _CTX = ctx                                   # ③ 赛制上下文
    _MODEL_REF = model                           # ④ 对手模型
    _OPP_JUMPED = _opp_bet_jumped(state)         # ⑤ 对手是否"突袭大注"
    try:
        _OPP_BETS_PER_HAND = float(model.avg_bets_per_hand()) \
            if model is not None else 0.9
    except Exception:
        _OPP_BETS_PER_HAND = 0.9                 # ⑥ 对手每手下注次数（窗口均值）
    _LEAD_LOCK = _is_lead_lock(state)            # ⑦ 优势锁定标志
```
**① 为什么一行写不下要用 `\`？** Python 用反斜杠表示"这一行还没结束"。

**为什么要专门抽一个函数？**
`decide()` 入口的锁赢拦截**早于** `_decide_impl()`。
这些全局原本只在 `_decide_impl` 里赋值 → 入口读到的是**上一手的旧值**。
典型后果：`_CTX` 过期 → doomed 判定用的档位偏移差 12 个大盲 → 边界判错。
抽成一个函数、入口和内层都调它，就再也不会不一致。

## 4.3 优势锁定：`_is_lead_lock()`

```python
def _is_lead_lock(state):
    try:
        return (int(state.total_win_chips[state.my_id])
                - int(state.total_win_chips[state.opp_id])) > LEAD_NO_ALLIN
    except Exception:
        return False
```
**逐行**：`total_win_chips` 是 `[我, 对手]` 的累计净盈亏。
差 > 2000（`LEAD_NO_ALLIN`）→ 进入锁定模式：**不再全押、注码受限**。
判定失败一律返回 `False`（不锁定）—— 保守方向。

## 4.4 锁赢线：`_lock_line()`

```python
def _lock_line(state):
    try:
        from game_state import INIT_CHIPS
        invested = INIT_CHIPS - int(state.my_chips)          # ① 本手已投入
        return 2 * (_blind_line(state, _hands_left(state), own=True) + invested)
    except Exception:
        return 0
```
**公式**：`锁赢线 = 2 × (本手已投 + 剩余手数的盲注线)`

**为什么乘 2？** 我弃牌输 X，对手就赢 X —— 一来一回，**领先差变化 2X**。
所以任何跟"领先差"比较的阈值都必须带这个 2×。

## 4.5 剩余手数：`_hands_left()`

```python
def _hands_left(state):
    try:
        h = int(state.hand_num)
        m = int(state.max_hand)
    except Exception:
        return 0
    if m <= 0:
        return 0
    if h >= m:                      # 兼容 1-based 平台 / 越界
        return 0
    return max(0, m - 1 - h)        # ① 本手之后还剩几手
```
**① 为什么是 `m - 1 - h`？**
平台 `hand` 是 **0-based**（0 ~ max_hand-1）。第 1 手是 `h = 0`，
所以"本手之后"的手数 = `(max_hand - 1) - h`。

**如果不减这个 1 会怎样**（真实事故）：
剩余手数多算一手 → 锁赢线虚高约 300 → **该弃牌保胜时不敢弃，反而继续加注**。
用户截图里"锁赢了还在加注"就是这个原因。

## 4.6 盲注线：`_blind_line()`

```python
def _blind_line(state, hands, own=True):
    try:
        sb, bb = 50, 100                    # ① 平台固定盲注（不用推导值）
        next_my_sb = state.my_id != state.dealer_id   # ② 下一手我是否小盲
        n_sb = (hands + 1) // 2 if next_my_sb else hands // 2
        n_bb = hands - n_sb                 # ③ 分别数小盲局/大盲局
        if own:
            return n_sb * sb + n_bb * bb    # ④ 我全程弃牌的损失
        return n_sb * bb + n_bb * sb        # ⑤ 对手全程弃牌时我能追回的量
    except Exception:
        return int(0.75 * 100 * hands)      # 兜底：平均每局 1.5BB
```
**② 位置每局轮换**：HU 里庄家每手交换，所以"当前是小盲"→"下一手是大盲"。
- `n_sb`：未来 `hands` 手里我当小盲的次数
- `n_bb`：当大盲的次数

**④⑤ 两种视角**
| 参数 | 含义 | 用途 |
|---|---|---|
| `own=True` | 我全程弃牌会损失多少（SB 局亏 50、BB 局亏 100）| 锁赢线（领先方安全线）|
| `own=False` | 对手全程弃牌时我能收多少盲注 | 追回线（落后方能否翻盘）|

**① 为什么硬编码 50/100？** 曾经用 `state.big_blind`（推导值），
在深投入时被推导成 250，导致追回线算成 375（真实 150）→ doom 边界误判。

## 4.7 锁胜判定：`_fold_out_active()`

```python
def _fold_out_active(state):
    try:
        lead = (state.total_win_chips[state.my_id]
                - state.total_win_chips[state.opp_id])   # ① 领先差
        return lead > _lock_line(state)                  # ② 超过锁赢线 → 锁赢
    except Exception:
        return False
```
**逻辑**：领先差 > `2×(本手已投 + 剩余盲注线)` →
即使本手弃牌、之后全程弃牌，仍然领先 → **锁定胜局**。

## 4.8 统一锁赢决策：`_lock_win_unified()`

```python
def _lock_win_unified(state):
    try:
        # A. 防锁赢：数学上追不回 → 只能 all-in 搏
        if _match_adjust(state) == "doomed":
            return {"act": "allin"}

        # B. 锁胜：领先到绝对安全 → 弃牌保胜
        if _fold_out_active(state):
            try:
                if int(state.to_call) <= 0:
                    return {"act": "check"}     # ① 无需跟注 → 不能弃牌，只能过牌
            except Exception:
                pass
            return {"act": "fold"}

        # C. 盈利锁胜：投入过大，输掉就把胜局送人 → all-in
        if not _is_lead_lock(state) and _profit_lock_allin(state):
            return {"act": "allin"}
    except Exception:
        pass
    return None      # 不触发 → 交给常规决策
```
**A / B / C 三个分支**
| 分支 | 含义 | 动作 |
|---|---|---|
| A `doomed` | 弃牌后对手就锁定胜局（追不回）| **全押**搏一把 |
| B 锁胜 | 我已经锁定胜局 | **弃牌**（或过牌）保胜 |
| C 盈利锁胜 | 本手投入太大，输了会被翻盘 | **全押**锁死利润 |

**① 的关键点**：`to_call == 0` 时**不能弃牌**（平台规则），
所以此时用 `check` —— 效果一样（本手不再投入），但一定合法。

## 4.9 兜底动作：`_safe_fallback_action()`

```python
def _safe_fallback_action(state):
    try:
        if _match_adjust(state) == "doomed":
            return {"act": "allin"}      # ① 弃牌=认输 → 只能拼
    except Exception:
        pass
    try:
        if int(state.to_call) <= 0:
            return {"act": "check"}      # ② 不用跟 → 过牌
    except Exception:
        pass
    return {"act": "fold"}               # ③ 其余 → 弃牌
```
**① 为什么必须先判 doomed？** 决策层抛异常时若正好是"弃牌就输"的局面，
无条件弃牌等于白送比赛。doomed 下唯一活路是全押。

## 4.10 总入口：`decide()`

```python
def decide(state, model, ctx=None, debug=False):
    # ① 日志开关（debug=True 或 环境变量 WB_POKER_DEBUG=1）
    try:
        import os
        DecisionLogger.enable(debug or os.environ.get("WB_POKER_DEBUG") == "1")
    except Exception:
        DecisionLogger.enable(debug)

    # ② 首次调用播报版本（自证场上跑的是哪颗 ELF）
    try:
        if DecisionLogger._enabled and not DecisionLogger._quiet:
            DecisionLogger.announce_once()
    except Exception:
        pass

    # ③ 记录本手结果（供"规则健康度"算 EV）
    try:
        DecisionLogger.note_state(state)
    except Exception:
        pass

    action = None
    _lw0 = _LW_UNSET

    # ④ 【M4】入口一次性刷新全部模块级全局
    try:
        _prepare_globals(state, model, ctx, reset_clock=True)
    except Exception:
        pass

    # ⑤ 【P0-3】硬性锁赢拦截：判中直接返回
    try:
        _lw0 = _lock_win_unified(state)
        if _lw0 is not None:
            action = _normalize(state, _lock_win_legal(state, _lw0))
    except Exception:
        action = None
        _lw0 = None

    # ⑥ 常规决策（异常 → 兜底动作）；把 _lw0 传进去避免重复计算
    if action is None:
        try:
            action = _decide_impl(state, model, ctx, debug=debug,
                                  _lw=_lw0, _prepared=True)
        except Exception:
            action = _safe_fallback_action(state)

    # ⑦ 归因日志（候选规则 / 采纳的规则 / 牌力 / 对手画像）
    try:
        if DecisionLogger._enabled:
            ...
            DecisionLogger.log(state.hand_num, state.stage, state.pot,
                               _rule, str(action.get("act")), ...)
            DecisionLogger.maybe_health(state)      # 每 10 手输出健康度
    except Exception:
        pass
    return action
```
**逐段说明**
| 段 | 作用 | 注意 |
|---|---|---|
| ① | 打开日志 | 日志写 **stderr**，不污染 stdout 协议 |
| ② | 版本播报 | `[BOT_VERSION] <短SHA>` —— 用来确认场上 ELF 版本 |
| ③ | 登记本手结果 | 供"规则健康度"统计 EV |
| ④ | **刷新全局** | 否则 ⑤ 读到上一手旧值 |
| ⑤ | **硬性拦截** | 锁胜/防锁赢不允许被后续分支覆盖 |
| ⑥ | 常规决策 | `_prepared=True` 表示"全局已刷过，别重复刷" |
| ⑦ | 归因日志 | 只记录，不影响决策 |

## 4.11 第一道安全网：`_normalize()`

```python
def _normalize(state, action):
    act = action.get("act", "fold")
    to_call = state.to_call
    my_left = state.my_left

    # ---- 情况1：本手有人全押 → 只允许 弃牌 / 全押 ----
    if state.any_allin:
        if _LEAD_LOCK:
            return {"act": "fold"}          # ① 优势锁定 → 对手全下也弃
        if act == "fold" and _match_adjust(state) == "doomed":
            return {"act": "allin"} if my_left > 0 else {"act": "fold"}
        if act == "fold":
            return {"act": "fold"}
        if _over_limit(state, my_left):
            return {"act": "fold"}          # ② 翻前跟全下超 1000 → 弃
        if state.stage != "preflop" and my_left > _bet_limit(state):
            return {"act": "fold"}          # ③ 翻后超牌型上限 → 弃
        return {"act": "allin"} if my_left > 0 else {"act": "fold"}

    # ---- 情况2：弃牌 ----
    if act == "fold":
        if not _LEAD_LOCK and _match_adjust(state) == "doomed":
            return {"act": "allin"} if my_left > 0 else {"act": "fold"}   # ④
        return {"act": "fold"}

    # ---- 情况3：过牌 ----
    if act == "check":
        return {"act": "check"} if to_call == 0 else {"act": "fold"}      # ⑤

    # ---- 情况4：全押 ----
    if act == "allin":
        if my_left > 0:
            if _over_limit(state, my_left):                              # ⑥
                if to_call > 0:
                    return {"act": "fold"}
                num = PREFLOP_MAX_BET
                ...
            return {"act": "allin"}
        return {"act": "check"} if to_call == 0 else {"act": "fold"}

    # ---- 情况5：跟注 ----
    if act == "call":
        if to_call <= 0:
            return {"act": "check"}                                      # ⑦
        if to_call >= my_left:
            ...                                                          # ⑧
        return {"act": "call"}
```
**逐条解释**
| 编号 | 规则 | 原因 |
|---|---|---|
| ① | 领先锁定时不跟全押 | 用户规则：领先 > 2000 不冒险 |
| ②③ | 投入超上限 → 弃 | "翻前 ≤1000 / 翻后按牌型上限"的用户规则 |
| ④ | doomed 时弃牌升级为全押 | 弃牌 = 直接认输 |
| ⑤ | `to_call > 0` 时不能过牌 | 必须跟/加/弃 |
| ⑥ | 全押超上限 → 降级为上限加注 | 保持游戏能继续（而不是被迫弃）|
| ⑦ | 无需跟注 → 跟注变过牌 | 等价且合法 |
| ⑧ | 筹码不够完整跟 → 全押 | 平台只接受"跟或全押" |

**整体设计**：决策层可以"想得很激进"，输出层一定"很保守"。
即便策略有 bug，也不会产生非法动作被判负。

---

# 第五部分：函数调用关系图

```
bot.run()                                   §1.8
 └─ _handle_line(obj)                       §1.7
     ├─ _persisted_str(...)                 §1.6
     ├─ _extract_request(obj)               §1.5
     ├─ game_state.parse_request(request)   §2.2
     │   └─ GameState._analyze()            §2.3
     ├─ OpponentModel.from_json(...)        §3
     ├─ build_model_from_history(...)       §3.4
     ├─ MatchContext.from_dict(...) / update() / sync_baseline()
     ├─ strategy.decide(state, model, ctx)  §4.10
     │   ├─ DecisionLogger.enable / announce_once / note_state
     │   ├─ _prepare_globals(...)           §4.2
     │   │   ├─ _opp_bet_jumped(state)
     │   │   └─ _is_lead_lock(state)        §4.3
     │   ├─ _lock_win_unified(state)        §4.8
     │   │   ├─ _match_adjust(state)  →  doomed?
     │   │   ├─ _fold_out_active(state)     §4.7
     │   │   │   └─ _lock_line(state)       §4.4
     │   │   │       ├─ _hands_left(state)  §4.5
     │   │   │       └─ _blind_line(...)    §4.6
     │   │   └─ _profit_lock_allin(state)
     │   ├─ _lock_win_legal(state, _lw0)
     │   ├─ _normalize(state, action)       §4.11
     │   └─ _decide_impl(...)               （常规决策，见《代码流程说明》）
     ├─ _to_response(action)                §1.2
     └─ _final_guard(state, resp)           §1.3
```

---

# 附录：常见"看不懂"的写法速查

| 写法 | 含义 |
|---|---|
| `a, b = 50, 100` | 同时给两个变量赋值 |
| `x or y` | `x` 为假（0/None/""/[]）时取 `y` |
| `x if cond else y` | 三元表达式：cond 为真取 x，否则取 y |
| `lst[-1]` | 倒数第一个元素（**危险**：越界不报错，会静默取错）|
| `obj.get(k, default)` | 取字典键，缺失时返回 default（不报错）|
| `for i, v in enumerate(lst)` | 同时拿到下标 `i` 和元素 `v` |
| `reversed(lst)` | 倒序遍历 |
| `isinstance(x, int)` | 判断类型 |
| `max(0, x)` | 把负数钳成 0 |
| `try/except` | 出错就走 except（本项目大量使用做防御）|
| `global X` | 在函数内修改模块级变量 X |
| `@property` | 让方法能像变量一样读取 |
| `object()` | 创建一个独一无二的对象（常做哨兵）|
| `\\`（行尾反斜杠）| 语句换行继续 |
| `f"{x}"` / `"%s" % x` | 字符串格式化（本项目用后者）|

---

# 一句话回顾

> **`bot.py` 负责"收发 + 保命"**（两层 try/except + 两道安全网）；
> **`game_state.py` 负责"翻译"**（把 JSON 变成 `to_call` 这些能用的数字）；
> **`opponent.py` / `match_ctx.py` 负责"记忆"**（对手什么风格、现在第几手）；
> **`strategy.py` 负责"思考"**（入口先刷全局 → 锁赢硬拦截 → 常规决策 → 安全网）。
