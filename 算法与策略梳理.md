# 德州扑克双人局 AI —— 算法与策略全景梳理

> 梳理对象：本仓库 `内容/` 下全部源码（`bot.py` / `game_state.py` / `cards.py` / `evaluator.py` / `ranges.py` / `equity.py` / `opponent.py` / `match_ctx.py` / `strategy.py`）。
> 说明：你 prompt 里把"规则三/规则二/规则一"列成了「偷盲 / 锁胜全下 / 下注额风控」，但**代码注释里的编号正好相反**（规则1=偷盲、规则2=锁胜全下、规则3=下注额风控）。下文以代码实际实现为准，并在括号里标注你 prompt 中的叫法，避免混淆。

---

## 一、算法与逻辑全景总览

| # | 技术手段 / 模块 | 所在文件 | 一句话作用 |
|---|----------------|----------|-----------|
| 1 | 牌编码与解析 | `cards.py` | 整数牌号 ↔ 字符串，牌库生成 |
| 2 | 牌型评估器 | `evaluator.py` | 7 选 5 全枚举，输出可比 6 元组 (category, t1..t5) |
| 3 | 起手牌强度/百分位 | `ranges.py` | Chen 公式（兼容）+ 单挑胜率标定 + 169 组合百分位表 |
| 4 | 蒙特卡洛胜率估算 | `equity.py` | 对抗对手范围抽样，含 `deadline` 软时限 |
| 5 | 平台协议解析与状态重建 | `game_state.py` | BotZone request → GameState（to_call/底池/合法性边界，盲注无关推导） |
| 6 | 对手行为画像统计 | `opponent.py` | VPIP/PFR/AF/弃牌率/大注率/Allin 频率 + 小样本收缩(shrinkage) |
| 7 | 对手原型分类 | `opponent.py` | station / maniac / rock / tag / unknown |
| 8 | 对手响应学习（尺寸-反应分桶） | `opponent.py` | 记录"我下注 X 后对手反应"，驱动下注尺寸与诈唬弃牌率 |
| 9 | 赛制上下文 | `match_ctx.py` | 对手弃牌推断 / 激进等级 / 回撤保护（只产阈值偏移） |
| 10 | 宏观状态机 | `strategy._match_adjust` | normal / protect / pressure / desperate / doomed / steal |
| 11 | 翻前决策 | `strategy._preflop_decide` | 百分位范围 + 位置 + 原型 + 状态机 |
| 12 | 翻后决策 | `strategy._postflop_decide` | 胜率分层 + 牌面纹理 + 听牌 outs + EV 门控 + 剥削 |
| 13 | 牌面纹理 / 听牌统计 | `strategy._board_texture` / `_count_outs` | 湿润度、outs 数 |
| 14 | 延迟施压 | `strategy._delayed_aggression_bonus` | 双方持续过牌后突然加注的弃牌率加成 |
| 15 | 公对风险规避 | `strategy.should_avoid_risk` / `_river_paired_trap` / `_risk_avoid_route` | 弱两对 / 河牌裸公对陷阱 |
| 16 | 锁胜弃牌 | `strategy._fold_out_active` | 领先足够大时全程弃牌拖到终局 |
| 17 | 优势锁定 LEAD_LOCK | `strategy.decide` 入口 | 领先>阈值：不 allin + 注码≤上限 |
| 18 | 规则1（你称"锁胜激进偷盲"） | steal 档（match_ctx + _match_adjust） | 对手疑似锁胜 → 任意牌高频偷盲，被加注即弃 |
| 19 | 规则2（你称"盈利锁胜全下"） | `strategy._profit_lock_allin` | 盈利且已深投入且牌力≥底线 → 全下锁胜 |
| 20 | 规则3（你称"下注额风控"） | `strategy._bet_cap_guard` + `_is_super_hand` | 非超强牌主动下注超过上限即降级 |
| 21 | 全下下限闸 | `strategy._allin_floor_guard` | 投入须 > 总盈利 + 常数 才允许 allin |
| 22 | 翻前全下决策 | `strategy._preflop_allin_decide` | 盈亏分档 + 极端赔率带 + 突袭保护 |
| 23 | 对手突袭大注检测 | `strategy._opp_bet_jumped` | 对手累计投入过大 → 收紧防守 |
| 24 | 钓鱼下注 | `strategy._fishy` + 各分支 | 跟注型对手用小注钓宽范围 |
| 25 | 合法性兜底 | `strategy._normalize` + `bot._final_guard` + `_fallback_resp` | 多层保底，永远输出合法动作 |
| 26 | 异常处理与超时保护 | `bot.run` 逐行模式 + 全局 try/except + MC deadline | 防崩溃/防超时被判非法 |

---

## 二、每个模块的详细说明

### 2.1 牌编码与解析（`cards.py`）
- **输入**：BotZone 牌字符串（如 `"SA"`）；内部整数 `card = rank*4 + suit`。
- **逻辑**：`parse_card` 用花色表+点数表转整数；`card_str` 还原；`full_deck` 生成 52 张；`rank/suit` 取分量。
- **输出**：整数牌 / 字符串 / 牌库。
- **位置**：所有模块的底层依赖。
- **参数**：`SUITS`(S0/H1/D2/C3)、`RANK_NAMES`(2..14, T=10)。
- **目的**：统一牌的表示，使 evaluator/ranges/equity 用同一编码。

### 2.2 牌型评估器（`evaluator.py`）
- **输入**：5~7 张整数牌。
- **逻辑**：`evaluate_7` 对 C(7,5)=21 组合全枚举，每组合 `evaluate_5` 判定 category（高牌0→皇家同花顺9）与 tiebreaker；含 A-2-3-4-5 轮子。`evaluate_5` 用点数计数+顺子/同花检测。
- **输出**：可比 6 元组，可直接 `<`/`>` 比大小。
- **位置**：被 equity、strategy（category 分层）调用。
- **参数**：10 个 category 常量（无可调阈值）。
- **目的**：牌力判定的事实标准；注释指出纯 Python 全枚举比位运算快（C 实现提速），非瓶颈。

### 2.3 起手牌强度与百分位（`ranges.py`）
- **输入**：两张底牌整数。
- **逻辑**：
  - `chen_score`（保留兼容）：点数表 + 同花+2 + 间隔惩罚。
  - `_raw_strength`：按单挑胜率表标定，对子 `50+2.9*(r-2)`，非对子 `33+1.6*hi+0.5*lo-间隔惩罚+同花/双高张加成`。
  - `_build_percentile_table`：模块加载时枚举 169 组合按强度排序，生成 `百分位 = (排名+0.5)/169`。
  - `hand_percentile`：返回 0~1（越小越强，AA≈0.003，72o≈0.997）。
- **输出**：百分位数值。
- **位置**：翻前范围决策（`_preflop_decide` 各分支）、蒙特卡洛对手范围抽样（`random_hand_in_range`）。
- **参数**：`_CHEN_POINTS`、`_GAP_PENALTY`、`_raw_strength` 中各系数（已固化，非运行时调节）。
- **目的**：用"百分位"替代 Chen 阈值作为范围语言，更贴合现代单挑理论（HU 庄家应开池 60~90%）。

### 2.4 蒙特卡洛胜率估算（`equity.py`）
- **输入**：我方 hole、board、迭代次数、对手范围 `opp_range_pct`、软时限 `deadline`。
- **逻辑**：
  ```
  for i in range(iterations):
      if deadline 到点(每16次查钟): break
      opp = 从"前 opp_range_pct 比例"随机抽两手（random_hand_in_range）
      runout = 剩余牌随机补到5张
      if evaluate_7(me) > evaluate_7(opp): wins++
      elif == : ties++
  return (wins + 0.5*ties) / total
  ```
- **输出**：平均胜率（含平局折半）。
- **位置**：翻后 `_postflop_decide`（1000 次）、翻前全下（600 次）、风险路线等。
- **参数**：默认 `iterations=500`、运行时 `MC_ITERATIONS=1000`、`TIME_BUDGET=0.5s`、`ALLIN_MC=600`、`ALLIN_TIME_BUDGET=0.30s`。
- **目的**：翻后牌力核心量化；软时限保证不超时（超时=非法=弃牌）。

### 2.5 平台协议解析与状态重建（`game_state.py`）
- **输入**：BotZone request dict。
- **逻辑**：
  - 牌号 `+8` 转内部编码；`current_round` 由公共牌数推导（0/1/2/3）。
  - `any_allin` 扫描历史。
  - **翻前**：用 `my_total_in=INIT_CHIPS-my_chips` 与对手最近动作精确推导 blind（仅用于尺寸）、`opp_round_bet`（raise-to 语义）、`to_call`。
  - **翻后**：仅重放当前轮 history 得本轮双方已投。
  - `pot = (INIT_CHIPS-my_chips)+(INIT_CHIPS-opp_chips)`。
  - `min_raise()`：有注→2×最大注；无注→大盲。`max_raise`：筹码上界。
- **输出**：GameState（含 `to_call/is_button/pot/effective_stack/curbet/...`）。
- **位置**：`bot._handle_line` 第一步。
- **参数**：`INIT_CHIPS=20000`、`DEFAULT_BIG_BLIND=200`（仅尺寸，不影响合法性）。
- **目的**：把平台 JSON 重建成决策所需状态；**所有影响合法性的量不依赖盲注常数**，即使盲注假设偏差也只影响 EV 不影响合法性。

### 2.6 对手行为画像统计（`opponent.py`）
- **输入**：来自 `build_model_from_history` 的对手每步动作（含去重、大注识别、街级 street）。
- **逻辑**：累计 `preflop_raise/call/fold`、`postflop_bet/call/fold/check`、`postflop_big_raise`、`allin_count`、`faces_bet`、`turn_raise/decisions`、`river_fold/faces`。`update()` 按 kind 落桶。
- **派生量（收缩后）**：`eff_vpip/eff_pfr/eff_fold_to_bet/eff_bet_freq/eff_big_raise/turn_aggr/river_fold_rate`，均经 `_shrunk(raw,n,key) = (raw*n + 先验*k)/(n+k)`。
- **输出**：收缩后的稳定统计。
- **位置**：每次 request 经 `build_model_from_history` 更新，`decide` 全程读取。
- **参数**：先验 `_PRIOR={vpip:0.68,pfr:0.30,af:0.35,fold_to_bet:0.40,big_raise:0.25,turn_aggr:0.35,river_fold:0.40}`；`_SHRINK_K=12`；`_DECAY_EVERY=15`（每 15 手指数衰减×0.5）。
- **目的**：用先验+收缩避免小样本噪声带偏；衰减让模型随比分快速遗忘旧策略。

### 2.7 对手原型分类（`opponent.archetype`）
- **输入**：`eff_vpip`、`eff_bet_freq`、样本量。
- **逻辑**：
  ```
  if sample_size<6: return unknown
  loose = vpip>0.60; aggro = af>0.42
  if loose and af>0.52: maniac
  if loose and not aggro: station
  if not loose and not aggro: rock
  if not loose and aggro: tag
  else: vpip>0.5 ? station : tag
  ```
- **输出**：原型字符串。
- **位置**：翻前/翻后各分支的剥削调节。
- **目的**：把连续统计聚类成可解释的对手类型，驱动范围伸缩与诈唬频率。

### 2.8 对手响应学习（尺寸-反应分桶）（`opponent.py`）
- **输入**：我方下注/加注事件 + 同手牌内对手反应（fold/call/raise）；跨手推断（我方非全押下注结尾→对手弃牌）。
- **逻辑**：
  - 桶定义：翻前 `总注额/大盲` → small≤2.5 / med≤4 / large>4；翻后 `总注额/底池` → small≤0.4 / med≤0.75 / large>0.75。
  - `bet_resp_stats(bucket)`：窗口内统计 fold/call/raise 率（`min_samples=3` 不足返回 None）。
  - `learned_fold_rate`：某尺寸下实测弃牌率，替代全局弃牌率。
  - `learned_value_bucket`：选"对手弃牌率最低"的桶 → 价值注避开对手爱弃的尺寸、钓最宽跟注范围。
  - `bucket_to_frac`：桶→代表比例（翻后 0.35/0.55/0.80，翻前 2.2/3.0/4.0）。
- **输出**：(桶, 弃牌率) / 弃牌率 / 代表尺寸。
- **位置**：价值注尺寸（`_learned_size`）、诈唬弃牌率（`_face_bet` 空气分支）。
- **参数**：`resp_window=20` 手；`min_samples=3`；桶阈值如上。
- **目的**：实证优先于原型假设——"对手弃牌率最低的桶"覆盖固定阈值，更准。

### 2.9 赛制上下文（`match_ctx.py`）
- **输入**：每手结算的净赢增量、对手是否全下。
- **逻辑（三模块，只产阈值偏移）**：
  1. **对手弃牌推断**：净赢增量 +50=对手 SB 弃、+100=对手 BB 弃；近 `WINDOW=20` 局 `direct_blind_rate>LOCK_RATE(0.65)` 且 `opp_allin_freq<LOCK_ALLIN_FREQ(0.10)` 且样本≥8 → `opponent_locking=True`。
  2. **激进等级**：`total_win_chips` 分档 ±20%→ 保守/正常/激进；`sync_baseline` 强制收敛（大盈≤保守、大亏≥激进）。
  3. **回撤保护**：单局亏损>15%→降档；连续盈利（保守3局/正常5局且盈亏>-10%）→升档。
  - `threshold_offset`：激进→-6BB、保守→+6BB（偏移到 `lead`）。
- **输出**：`opponent_locking` 标志、当前 `level`、阈值偏移量。
- **位置**：`bot` 层恢复→`ctx.update`→`ctx.sync_baseline`→传入 `decide`；`_match_adjust` 读取。
- **参数**：`WINDOW=20, BLIND_NETS=(50,100), LOCK_RATE=0.65, LOCK_ALLIN_FREQ=0.10, LOCK_MIN_SAMPLES=8, BRACKET_PCT=0.20, DRAWDOWN_PCT=0.15, UP_CONSEC_CONSERVATIVE=3, UP_CONSEC_NORMAL=5, UP_NORMAL_MIN_PNL=-0.10, LEVEL_SHIFT_BB=6`。
- **目的**：把"比赛节奏/对手是否锁胜"转成状态机阈值偏移，不新增独立决策路径。

### 2.10 宏观状态机（`strategy._match_adjust`）
- **输入**：`lead=我方净赢-对手净赢`、`hands_left`、`_CTX`。
- **逻辑**：
  ```
  if ctx.opponent_locking: return "steal"            # 模块一优先
  lead += ctx.threshold_offset()*bb                   # 模块二三偏移
  if lead-2*bb <= -2.5*bb*max(hands_left-1,1): return "doomed"  # 数学不可追
  if hands_left<=15:
      if lead>=30bb and opp短码: "pressure"
      elif lead>=30bb: "protect"
      elif lead<=-30bb: "desperate"
  return "normal"
  ```
- **输出**：状态字符串。
- **位置**：`_preflop_decide` / `_postflop_decide` 入口及各子分支。
- **目的**：按"领先量×剩余手数"切换风险偏好（保收益 vs 追分）。

### 2.11 翻前决策（`strategy._preflop_decide` + 子函数）
- **输入**：`pct=hand_percentile`、`arch`、`adj`、`state`。
- **分支**：
  - 对手全下/跟注即全下 → `_preflop_allin_decide`（见 2.22）。
  - 庄家位（`is_button`）：面对大盲→`_button_open`；面对 3-bet→`_button_vs_3bet`。
  - 大盲位：可选项（对手溜入）→`_bb_option`；需跟注→`_bb_defend`。
- **关键逻辑**：
  - `_button_open`：steal 档任意牌 2.5BB 开池；否则按原型伸缩 `open_pct`（rock 0.92/station 0.85/maniac 0.75），protect 收紧、desperate/doomed 全开；`pct<=open_pct` 开池（尺寸学习优先/2.2~2.5BB），否则垃圾牌偶尔溜入。
  - `_button_vs_3bet`：价值 4-bet（JJ+/AK，maniac 放宽到 8%，pressure/desperate 再放宽）；诈唬 4-bet（Axs 阻挡牌）；跟注范围 `clamp(1-2*required,0.10,0.60)` 并保底 ≥required，原型/状态机/突袭修正。
  - `_bb_option`：隔离加注 `ISO_SIZE_BB=3.2BB`（station 0.30/rock 0.55/steal 0.90/desperate 0.80），否则 check。
  - `_bb_defend`：防守范围 `clamp(1.15-1.8*required,0.22,0.90)`；3-bet 极化（基础 0.13，对手高弃牌率→0.20，station 0.10，desperate 0.35）；突袭时防守减半；贵跟注只守上半区。
- **输出**：开池/3-bet/4-bet/call/fold/check。
- **位置**：`decide` 的 `state.stage=="preflop"` 分支。
- **参数**：见第四节表。
- **目的**：用百分位范围做翻前开放/防守/反加注，取代了粗 Chen 阈值。

### 2.12 翻后决策（`strategy._postflop_decide` / `_check_side` / `_face_bet`）
- **输入**：state、model、`adj`、`arch`、`tex`（纹理）、`eq`（MC）、`category`、`outs`。
- **强度分层**：
  ```
  weak_pair = should_avoid_risk(...)
  strong = (category>=TWO_PAIR and not weak_pair) or eq>=0.78
  good    = not strong and eq>=0.60
  medium  = not good and eq>=0.50
  big_draw= not river and outs>=8
  draw    = not river and outs>=4
  ```
- **主动下注侧 `_check_side`**：
  - steal 档（非 strong/good）→ 0.55 池 C-Bet。
  - strong：OOP+对手激进→check（诱敌）；河牌坚果→超池；坚果级大注；否则价值注（学习优先/跟注型钓鱼小注/湿面大注）。
  - good：SPR≤2.5→0.6池；否则价值注（学习/钓鱼/薄价值）。
  - big_draw：半诈唬（fold_eq≥0.30 或 有位置 或 追分）。
  - medium：有位置湿面/主动方→小注施压；OOP 极干面→1/3池保护；否则 check。
  - draw：免费看牌。
  - 空气：纯诈唬 EV 门控 `fold_eq >= breakeven(0.355)+buffer`（阻挡牌 0.03/0.06/0.12，protect+0.15，desperate-0.12，延迟加成，学习弃牌率）。
- **面对下注侧 `_face_bet`**：
  - 隐含赔率（big_draw+深筹 → eff_req 降 1.4×）、原型/街级/状态机修正的 `eff_req+margin`。
  - steal 档被加注→弃（顺子+例外）；突袭大注→抬高门槛+eq 打折。
  - 河牌裸公对陷阱→硬弃。
  - 强牌按 SPR/牌力分层全下；良好牌价值加注或赔率跟注；听牌半诈唬/跟注；中等只打好赔率；空气罕见反诈唬。
- **输出**：完整动作 dict。
- **位置**：`decide` 的翻后分支。
- **目的**：把"胜率+牌型+位置+对手+状态机"综合成价值/诈唬/控池决策。

### 2.13 牌面纹理 / 听牌统计（`_board_texture` / `_count_outs`）
- **纹理**：`wet` 累加（三同花+0.35、双色+0.15、连通≥3+0.25、四连+0.15、对子面+0.10、4张+0.05，封顶1）；`high=(顶牌-2)/12`。
- **outs**：同花听+9；顺子每缺档+4（卡顺4、两头8），封顶17。
- **位置**：翻后决策的尺寸与半诈唬判定。
- **目的**：湿面大注拒绝给听牌好赔率，干面小注/诈唬。

### 2.14 延迟施压（`_delayed_aggression_bonus`）
- **输入**：history、current_round。
- **逻辑**：对手连续 check≥2 次 → 返回 `OPP_CHECK_BONUS=0.30`（直接加注）；上/上两街双方都 check 且我方首动 → `DELAYED_BONUS_PARTIAL=0.10`，三街持续 → `DELAYED_BONUS_FULL=0.20`。
- **输出**：fold_eq 加成 0~0.30。
- **位置**：`_check_side` 空气分支、诈唬 fold_eq 计算。
- **目的**：双方过牌后突然出手弃牌率显著高于均值，提升诈唬 EV。

### 2.15 公对风险规避（`should_avoid_risk` / `_river_paired_trap` / `_risk_avoid_route`）
- **should_avoid_risk**：3~4 张公共牌且有公对；我方构成"底部两对"（手牌对<公对）或踢脚<公面高张 → True。
- **_river_paired_trap**：河牌、≥3 不同点数且无 A、有公对、我方仅裸公对（对子全来自公面）→ 公面有更高单张 或 公对<T → 硬弃。
- **_risk_avoid_route**：领先→只 check/小跟（≤50%池），对手全下即弃；大幅落后且剩局少→胜率>25%才跟全下；否则降级为普通一对评估（strong 已排除弱两对，最多 good/medium）。
- **位置**：`decide` 中优先级高于常规翻前/翻后（第 4 道 gate）。
- **参数**：`RISK_LEAD_CALL_FRAC=0.50, RISK_ALLIN_EQ_FLOOR=0.25, RISK_BEHIND_LIMIT=-4000, RISK_HANDS_LEFT=20, RIVER_TRAP_KICKER=12, RIVER_TRAP_WARN_KICKER=14, RIVER_TRAP_WARN_MARGIN=0.15`。
- **目的**：防"小优势 allin 被三条/更高两对统治"与"河牌裸公对几乎必输"。

### 2.16 锁胜弃牌（`_fold_out_active`）
- **逻辑**：`lead > 2.5 * big_blind * hands_left` → 全程弃牌后领先仍为正 → 直接弃。
- **输出**：bool。
- **位置**：`decide` 第 3 道 gate（最高优先级之一）。
- **目的**：零方差锁定胜局，后期大领先时避免波动输。

### 2.17 优势锁定 LEAD_LOCK（`decide` 入口）
- **逻辑**：`_LEAD_LOCK = (我净赢-对手净赢) > LEAD_NO_ALLIN(2000)`。一旦为真：不 allin、单次注码≤`LEAD_MAX_BET(1000)`；对手全下也弃（不 allin）。
- **位置**：`decide` 最开头设置，贯穿所有 gate 与 `_normalize`。
- **目的**：领先时只用小注慢慢积累，避免大注送筹码（用户规则，优先级最高）。

### 2.18 规则1 —— 偷盲档（你称"锁胜激进偷盲"）
- **触发**：`match_ctx.opponent_locking`（对手近20局收盲率>65% 且全下率<10%）。
- **表现**：
  - `_match_adjust` → `"steal"`。
  - 翻前 `_button_open`：任意两张牌 `STEAL_OPEN_BB=2.5BB` 开池，频率 100%。
  - 翻后 `_check_side`：非 strong/good 一律 0.55 池 C-Bet（频率约 85%）。
  - `_face_bet`：被加注/全下立即弃（顺子+ 例外）。
  - `_bet_cap_guard` 对 steal 档**不生效**（高频 C-Bet 可超 1000）。
- **目的**：对手疑似锁胜（收紧不乱动）→ 用高频小额偷盲追分。

### 2.19 规则2 —— 盈利锁胜全下（你称"盈利锁胜全下"）
- **逻辑**：`pnl>0` 且 `invested(=INIT_CHIPS-my_chips) > pnl+PROFIT_LOCK_CONST(2000)` 且 `MC胜率 vs 随机牌 > PROFIT_LOCK_EQ(0.30)` → 全下。
- **位置**：`decide` 第 4 道 gate，仅当 `not _LEAD_LOCK`。
- **参数**：`PROFIT_LOCK_CONST=2000, PROFIT_LOCK_EQ=0.30, ALLIN_MC=600, ALLIN_TIME_BUDGET=0.30`。
- **目的**：已深投入且牌力有底线时直接推掉，防止利润后街被反超。

### 2.20 规则3 —— 下注额限制（你称"下注额风控"）
- **逻辑**：动作是 raise 且非超强牌（`_is_super_hand`：JJ+/AKs）→ 总注额 `num>BET_CAP(1000)` 时降级：底池≤`BET_CAP_POT(2000)`→50%池；否则放弃主动下注（跟/过）。
- **位置**：`decide` 末段，steal 档跳过。
- **参数**：`BET_CAP=1000, BET_CAP_POT=2000, BET_CAP_FRAC=0.50, _is_super_hand 阈值(JJ+/AKs)`。
- **目的**：防边缘牌投入过多筹码。

### 2.21 全下下限闸（`_allin_floor_guard`）
- **逻辑**：动作是 allin 时：先判 `_LEAD_LOCK`→否 allin；否则 `invest（累计投入=已投+跟注额 或 =my_left）> total_win_chips[我]+ALLIN_FLOOR_CONST(1000)` 才放行；否则能过则过、否则弃。
- **位置**：`decide` 中常规决策后、规则3 前。
- **参数**：`ALLIN_FLOOR_CONST=1000`。
- **目的**：盈利越高允许 allin 门槛越高（领先保护）；落后时门槛为负几乎不限（搏翻盘）。

### 2.22 翻前全下决策（`_preflop_allin_decide`）
- **输入**：hole、state、model、原型、累计盈亏、突袭标志。
- **逻辑**：
  ```
  rp = 对手翻前范围 * {maniac:0.90,station:0.70,tag:0.60,rock:0.35}（clamp 0.02~1）
  eq = MC(hole,[],ALLIN_MC,rp)
  required = call_amt / (pot+call_amt)
  if eq >= required+0.10: return allin        # 第四步：极端好赔率无条件跟
  if eq <= required-0.10: return fold          # 第四步：极端差赔率无条件弃
  # 第三步：按盈亏分档取门槛
  lead>50BB: big_lead(0.75); >10BB: lead(0.65); >-10BB: even(0.55);
  >-50BB: behind(0.50); else: big_behind(0.40)
  终局(剩≤15手)修正: 领先+0.03 / 落后-0.03
  突袭: thr=min(0.90, thr+0.10)
  领先: thr=max(thr, required)
  return eq>=thr ? allin : fold
  ```
- **输出**：fold / allin（平台规则有人全下只许 -1/-2）。
- **位置**：`_preflop_decide` 首判（对手全下/跟注即全下）。
- **参数**：`ALLIN_MC=600, ALLIN_BAND_PCT=0.10, ALLIN_LEAD_BB=50, ALLIN_SMALL_BB=10, ALLIN_THR={...}, ALLIN_ENDGAME_SHIFT=0.03, OPP_JUMP_FAC_MARGIN=0.10`。
- **目的**：按盈亏动态分档，避免"优势下跟 all-in 比运气"；极端赔率优先防反向剥削。

### 2.23 对手突袭大注检测（`_opp_bet_jumped`）
- **逻辑**：重放 history，按轮跟踪双方已投（raise-to 语义），累计对手本手全部投入（call+raise）；`opp_cum > OPP_JUMP_THRESHOLD(2000)` → 突袭。
- **位置**：`decide` 入口设置 `_OPP_JUMPED`，翻前全下/翻后跟注/4-bet/3-bet 各分支读取。
- **参数**：`OPP_JUMP_THRESHOLD=2000`（注释提到"增量>4倍"但实现为累计>2000，见第五节冲突）。
- **目的**：对手突然大幅加注=强牌信号，收紧跟注/加注范围、抬高门槛。

### 2.24 钓鱼下注（`_fishy`）
- **逻辑**：`archetype()=="station"` 或 `eff_fold_to_bet() < FISH_FOLD_BB(0.35)` → 跟注型。
- **效果**：价值注改用小注（`FISH_BET=0.45 / WET 0.50 / DRY 0.38`），加注用 `FISH_RAISE=0.45`，钓更宽跟注范围；坚果级仍大注榨取。
- **位置**：翻后价值分支、加注构造 `_raise_pot(fish=...)`。
- **参数**：`FISH_FOLD_BB=0.35, FISH_BET=0.45, FISH_BET_WET=0.50, FISH_BET_DRY=0.38, FISH_RAISE=0.45`。
- **目的**：跟注型对手不按比例弃牌，小注反而利润更高。

### 2.25 合法性兜底（多层）
- **`strategy._normalize`**：规则5（有人全押只许 fold/allin）；check 仅当 to_call==0；call 需严格 my_left>to_call；raise 夹紧到 `[min_raise, max_raise)`，越界转 allin；**全局加注增量 ≤ `MAX_RAISE_INCR(1000)`**；LEAD_LOCK 时注码≤`LEAD_MAX_BET` 且对手全下也弃。
- **`bot._final_guard`**：整数 response 层面再校验一遍（同样的规则5、min/max raise、call 严格大于）。
- **`bot._fallback_resp`**：决策层异常时"能过则过、能跟则跟、否则弃"。
- **目的**：任何上游 bug 或异常都保证输出合法，平台不会判非法。

### 2.26 异常处理与超时保护
- **超时**：MC 循环每 16 次查 `time.time()>=deadline` 提前收敛（`TIME_BUDGET=0.5s`，平台 1s 限，留 20 倍余量）；`_profit_lock_allin` 用 0.25s 子预算。
- **崩溃**：`bot._handle_line` 内外双层 try/except，异常落 `_fallback_resp`；`_normalize`/`_final_guard` 内部也 try/except。
- **I/O**：`bot.run` **逐行读取 stdin**（修复原 `read()` 等 EOF 在常驻模式下 8s 无输出的预检失败）；非 JSON 输入忽略；输出后 `flush()`。
- **脏数据**：`OpponentModel.from_json`/`MatchContext.from_dict` 只吸收已知字段，JSON 往返的 str key 统一转 int。
- **目的**：在 1 秒限、平台常驻/重启两种模式下都绝不超时、绝不崩溃、绝不非法。

---

## 三、决策流与模块调用顺序

```
[BotZone 平台] 逐行推送 request JSON
        │
        ▼
bot.run()  ── 逐行读取，非 JSON 忽略，输出 flush
        │
        ▼
_handle_line(obj)
   ├─ _extract_request → 取本轮 request dict
   └─ try:
        │
        ▼
   parse_request → GameState._analyze()
        │   （状态重建：to_call / pot / 合法性边界 / 盲注推导）
        ▼
   model = OpponentModel.from_json(globaldata or data)
   build_model_from_history(model, request, state)
        │   （增量统计对手动作 + 大注识别 + 响应学习分桶 + 衰减）
        ▼
   ctx = MatchContext.from_dict(model.ctx_dict)
   ctx.update(state)        ── 结算上一手（弃牌率/全下率/激进等级）
   ctx.sync_baseline(state) ── 激进档向盈亏分档收敛
        │
        ▼
   decide(state, model, ctx)
        │
        ├─[1] 设置 _OPP_JUMPED = _opp_bet_jumped(state)      （全局，最高优先级共享）
        ├─[2] 设置 _LEAD_LOCK = lead > LEAD_NO_ALLIN(2000)    （最高优先级硬规则）
        │
        ├─[3] if _fold_out_active(state):  return fold        （锁胜弃牌，零方差）
        │
        ├─[4] if not _LEAD_LOCK and _profit_lock_allin(state): return allin  （规则2）
        │
        ├─[5] if should_avoid_risk(state):  action = _risk_avoid_route(...) （公对风险）
        │     elif preflop:  action = _preflop_decide(...)
        │     else:          action = _postflop_decide(...)
        │            │
        │            │  _preflop_decide / _postflop_decide 内部：
        │            ├─ adj = _match_adjust(state)  → normal/protect/pressure/desperate/doomed/steal
        │            ├─ arch = model.archetype()
        │            ├─ eq = monte_carlo_equity(...)   （翻后）
        │            ├─ category = evaluate_7(...) ； outs = _count_outs(...)
        │            ├─ 各分支结合 原型/状态机/纹理/学习尺寸/突袭标志 产出动作
        │
        ├─[6] action = _allin_floor_guard(state, action)     （全下下限闸）
        │
        ├─[7] if _match_adjust(state) != "steal":
        │        action = _bet_cap_guard(state, action)       （规则3，steal 跳过）
        │
        └─[8] return _normalize(state, action)               （合法性兜底+增量上限+LEAD_LOCK）
        │
        ▼
   _to_response(action) → 整数 (-1/-2/0/>0)
   resp = _final_guard(state, resp)   （整数层面再校验）
   model.ctx_dict = ctx.to_dict()
   data_out = model.to_json()         （持久化对手模型+赛制上下文）
        │
        ▼
   输出 {"response":resp,"data":data_out,"globaldata":data_out}
```

**优先级要点**：入口的 `_LEAD_LOCK` 与 `_OPP_JUMPED` 在一切之前生效；其后 8 道 gate 顺序为 锁胜弃牌 → 规则2全下 → 公对风险 → 常规决策 → 全下下限 → 规则3 → 合法化。规则1(steal) 由 `_match_adjust` 在常规决策内部生效，故只影响翻前/翻后分支、不触发时不限制规则2/3。

---

## 四、参数配置总表

### 4.1 翻前范围（`strategy.py` 顶部）
| 参数 | 默认 | 含义 / 影响 |
|------|------|------------|
| `BTN_OPEN_PCT` | 0.80 | 庄家开池基准百分位（HU 标准宽度） |
| `OPEN_SIZE_BB` | 2.5 | 开池尺寸（raise-to，大盲倍数）；最小加注 2BB |
| `OPEN_SIZE_VS_STATION` | 2.2 | 对跟注站小尺寸开池（反正不弃） |
| `BB_3BET_PCT` | 0.13 | 大盲 3-bet 基准范围 |
| `BB_3BET_BLUFFY` | 0.20 | 对手高弃牌率时诈唬性 3-bet 范围 |
| `ISO_SIZE_BB` | 3.2 | 大盲隔离加注尺寸 |
| `SB_4BET_VALUE_PCT` | 0.035 | 价值 4-bet（JJ+/AK） |
| `SB_4BET_MULT` | 2.3 | 4-bet 尺寸 = 对手 3-bet 额 × 此倍 |

### 4.2 翻后下注尺寸（底池比例）
| 参数 | 默认 | 含义 |
|------|------|------|
| `VALUE_BET` | 0.65 | 常规价值下注 |
| `VALUE_BET_WET` | 0.75 | 湿润面大注锁听牌 |
| `THIN_VALUE` | 0.55 | 薄价值 / 中强牌 |
| `OVERBET` | 1.30 | 河牌坚果超池榨取 |
| `BLOCKER_BET` | 0.33 | 干面 OOP 小注保护 |
| `CBET` | 0.55 | 持续下注 / 半诈唬 |
| `BLUFF` | 0.55 | 纯诈唬 |
| `BLOCKER` | 0.45 | 有位置小注施压 |

### 4.3 计算预算
| 参数 | 默认 | 含义 |
|------|------|------|
| `MC_ITERATIONS` | 1000 | 翻后 MC 抽样数 |
| `TIME_BUDGET` | 0.5s | 翻后决策软时限（平台 1s 限） |
| `ALLIN_MC` | 600 | 翻前全下 MC 抽样 |
| `ALLIN_TIME_BUDGET` | 0.30s | 翻前全下软时限 |

### 4.4 翻前全下分档（`_preflop_allin_decide`）
| 参数 | 默认 | 含义 |
|------|------|------|
| `ALLIN_BAND_PCT` | 0.10 | 极端赔率带宽（±10% 无条件跟/弃） |
| `ALLIN_LEAD_BB` | 50 | 大幅领先阈值（BB） |
| `ALLIN_SMALL_BB` | 10 | 小幅领先/落后阈值（BB） |
| `ALLIN_THR` | big_lead .75 / lead .65 / even .55 / behind .50 / big_behind .40 | 各盈亏档跟注胜率门槛 |
| `ALLIN_ENDGAME_SHIFT` | 0.03 | 终局（剩≤15手）修正 |

### 4.5 钓鱼下注
| 参数 | 默认 | 含义 |
|------|------|------|
| `FISH_FOLD_BB` | 0.35 | 对手弃牌率<此值→跟注型（钓鱼目标） |
| `FISH_BET` | 0.45 | 跟注型常规价值注（常规 0.65→0.45） |
| `FISH_BET_WET` | 0.50 | 湿润面 |
| `FISH_BET_DRY` | 0.38 | 干燥面 |
| `FISH_RAISE` | 0.45 | 面对加注的克制尺寸（常规 0.75→0.45） |

### 4.6 公对风险规避
| 参数 | 默认 | 含义 |
|------|------|------|
| `RISK_LEAD_CALL_FRAC` | 0.50 | 领先时弱两对最多跟 50% 池 |
| `RISK_ALLIN_EQ_FLOOR` | 0.25 | 大幅落后跟全下胜率底线 |
| `RISK_BEHIND_LIMIT` | -4000 | 大幅落后阈值（累计净赢） |
| `RISK_HANDS_LEFT` | 20 | 落后场景剩余局数上限 |
| `RIVER_TRAP_KICKER` | 12 | 踢脚<Q→硬弃（不计算） |
| `RIVER_TRAP_WARN_KICKER` | 14 | 踢脚< A→warning（高门槛） |
| `RIVER_TRAP_WARN_MARGIN` | 0.15 | warning 档额外胜率门槛 |

### 4.7 优势锁定 / 全下下限 / 规则2 / 规则3
| 参数 | 默认 | 含义 |
|------|------|------|
| `LEAD_NO_ALLIN` | 2000 | 领先阈值：超过进锁定模式 |
| `LEAD_MAX_BET` | 1000 | 锁定模式单次注码上限（10BB） |
| `MAX_RAISE_INCR` | 1000 | 加注增量（跟平外额外）上限（全局） |
| `ALLIN_FLOOR_CONST` | 1000 | 全下下限常数（投入须>总盈利+此值） |
| `PROFIT_LOCK_CONST` | 2000 | 规则2：已投须超 盈亏+此值 |
| `PROFIT_LOCK_EQ` | 0.30 | 规则2：对抗随机牌胜率底线 |
| `BET_CAP` | 1000 | 规则3：非超强牌主动下注总注额上限 |
| `BET_CAP_POT` | 2000 | 规则3：底池超此值完全放弃主动下注 |
| `BET_CAP_FRAC` | 0.50 | 规则3：降级后下注（底池 50%） |

### 4.8 突袭大注 / 延迟施压 / 规则1偷盲
| 参数 | 默认 | 含义 |
|------|------|------|
| `OPP_JUMP_THRESHOLD` | 2000 | 对手本手累计已下注超此值→跟注警告 |
| `OPP_JUMP_FAC_MARGIN` | 0.10 | 突袭时跟全下门槛额外抬高 |
| `OPP_JUMP_EQ_PENALTY` | 0.15 | 突袭时 eq 打折 |
| `DELAYED_BONUS_FULL` | 0.20 | 三街持续过牌后突然出手加成 |
| `DELAYED_BONUS_PARTIAL` | 0.10 | 转牌突然出手加成 |
| `OPP_CHECK_BONUS` | 0.30 | 对手连过≥2次直接加注加成 |
| `STEAL_OPEN_BB` | 2.5 | 偷盲开池尺寸 |
| `STEAL_CBET_FRAC` | 0.55 | 偷盲档 C-Bet 比例 |
| `STEAL_FOLD_MIN_CAT` | 4 | 偷盲被加注时顺子+仍正常决策 |

### 4.9 对手建模（`opponent.py`）
| 参数 | 默认 | 含义 |
|------|------|------|
| `_PRIOR` | vpip .68/pfr .30/af .35/fold_to_bet .40/big_raise .25/turn_aggr .35/river_fold .40 | 先验（HU 松弱口径） |
| `_SHRINK_K` | 12 | 收缩强度（约12次观测后数据为主） |
| `_DECAY_EVERY` | 15 | 每15手指数衰减（旧数据×0.5） |
| `resp_window` | 20 | 响应学习窗口（手牌数） |
| 桶阈值 | 翻前 ≤2.5/≤4/>4；翻后 ≤0.4/≤0.75/>0.75 | 尺寸分桶 |

### 4.10 赛制上下文（`match_ctx.py`）
| 参数 | 默认 | 含义 |
|------|------|------|
| `WINDOW` | 20 | 滑动窗口（近20局） |
| `BLIND_NETS` | (50,100) | 直接收盲净赢（对手弃SB/BB） |
| `LOCK_RATE` | 0.65 | 收盲率>此值→疑似锁胜 |
| `LOCK_ALLIN_FREQ` | 0.10 | 全下率<此值→疑似锁胜 |
| `LOCK_MIN_SAMPLES` | 8 | 判定锁胜最少样本 |
| `BRACKET_PCT` | 0.20 | 盈亏±20%分激进等级 |
| `DRAWDOWN_PCT` | 0.15 | 单局亏损>此值降档 |
| `UP_CONSEC_CONSERVATIVE` | 3 | 保守档升档连续盈利局 |
| `UP_CONSEC_NORMAL` | 5 | 正常档升档连续盈利局 |
| `UP_NORMAL_MIN_PNL` | -0.10 | 正常档升档盈亏下限 |
| `LEVEL_SHIFT_BB` | 6 | 激进等级对状态机偏移（BB） |

### 4.11 平台 / 基础（`game_state.py` / `cards.py`）
| 参数 | 默认 | 含义 |
|------|------|------|
| `INIT_CHIPS` | 20000 | 每手重置初始筹码 |
| `DEFAULT_BIG_BLIND` | 200 | 大盲默认（仅尺寸，不影响合法性） |

---

## 五、潜在问题与改进建议

### 5.1 🔴 严重逻辑缺陷：`_face_bet` 面对 all-in 的分支
代码（约 1228–1239 行）：
```
if state.opp_is_allin or to_call >= state.my_left:
    if adj == "pressure":
        thr = eff_req - 0.05
    elif adj in ("desperate","doomed"):
        thr = eff_req - 0.08
    else:
        thr = eff_req + margin
        return {"act": "allin"}     # ← 直接返回，从未用 thr 比较 eq
    return {"act": "fold"}          # ← pressure/desperate/doomed 永远落这里
```
**问题**：`thr` 算出来却从未与 `eq` 比较。
- **normal 档**：对手一 all-in（或我跟注即全下）就**无条件 call all-in**，哪怕手里是空气——任何河牌/转牌被 shove 都接，巨大 leak。
- **pressure / desperate / doomed 档**：反而**永远 fold** 给 all-in，即使拿着坚果也不接——追分档本该大胆接全下。
两个方向都错且相反，与注释"需要胜率 > 赔率要求的边际"完全矛盾。
**缓解**：目前 `_allin_floor_guard` 会在"call 额小"时把它翻成 fold，所以小注 shove 的 normal 接被部分拦下；但**大注 shove 的 normal 仍会无脑接**，且**desperate/doomed 对 all-in 永远弃**完全没被纠正（guard 只处理 allin 动作，不处理 fold）。
**修复建议**：改为 `if eq >= thr: return allin; return fold`，并对 desperate/doomed 用更松的 thr（已算好 `eff_req-0.08`，只需真正用于比较）。

### 5.2 🟠 阈值硬编码与盲注脱节
`LEAD_NO_ALLIN=2000`、`ALLIN_FLOOR_CONST=1000`、`BET_CAP=1000`、`MAX_RAISE_INCR=1000` 等是**绝对筹码值**，隐含假设大盲=100（`big_blind` 在 game_state 里默认 200，实际由平台推导，HU 通常为 50/100）。若比赛盲注不同（如 100/200 或动态升降），这些固定阈值会整体漂移——领先锁定的"2000"在盲注翻倍后可能过早/过晚触发。建议全部改为 **BB 倍数**（如 `LEAD_NO_ALLIN_BB=20` → `20*bb`）。

### 5.3 🟠 注释与实现不一致：突袭大注判定
`_opp_bet_jumped` 实际实现仅 `opp_cum > OPP_JUMP_THRESHOLD(2000)`（对手本手**累计**投入>2000），但多处注释写"增量 > 4× 此前累计""对手突然大幅加注（799→5300）"。当前实现没有"4倍增量"逻辑，而是单纯累计阈值。这会让"突袭"在对手**平稳深注**时也触发（并非真正的 sudden jump），可能过度收紧正常深筹对抗。建议要么实现真正的"增量倍率"判定，要么修正注释。

### 5.4 🟡 河牌裸公对陷阱偏保守
`_river_paired_trap` 硬弃（不计算）会**放弃一些实际上领先的情形**（例如我持有顶踢脚、对手只是用更差的一对跟注），纯防守牺牲了价值。这是安全的但有 EV 损失。可改为：仅在 `对手范围含大量两对/三条` 且 `我方胜率经一次轻量 MC < 某阈值` 时才弃，而非纯规则硬弃。

### 5.5 🟡 LEAD_LOCK 与 fold-out 之间存在"过保守窗口"
LEAD_LOCK 在 `lead>2000`（约 20BB）即触发，而 fold-out 需要 `lead>2.5*bb*hands_left`（70 手赛、剩 70 手时约 17500）。中间一大段（lead 2000~17500）机器人处于"不 allin、注码≤1000"的强保守态。若对手在后期靠偷盲快速追分，这种保守可能放跑翻盘机会。建议在 lead 处于中区时保留"薄价值全下/适度加注"的弹性，而非一刀切锁死。

### 5.6 🟡 `_profit_lock_allin` 每手额外跑一次 MC
只要 `pnl>0 且 not LEAD_LOCK`，每次决策都会进入 `_profit_lock_allin` 并做 `invested>pnl+2000` 判断；多数情况快速返回 False，但接近阈值时会跑 600 次 MC（0.25s）。叠加翻后 1000 次 MC，单步最坏可能接近 0.75s，逼近 1s 软上限。建议在 `_postflop_decide` 已算过 `eq` 时复用，或把预算统一调度。

### 5.7 🟡 对手模型先验偏松
`_PRIOR` 把 VPIP 设为 0.68、PFR 0.30（松弱口径）。若实际对手偏紧（如岩石 AI），前 12 手会被先验拖着做出过宽的开池/跟注，直到收缩生效。可考虑按"天梯分布"做更中性的先验，或加快收缩（`_SHRINK_K` 从 12 降到 8）。

### 5.8 优先改进建议（按 ROI 排序）
1. **立即修 5.1 的 `_face_bet` all-in 分支**——这是确定性的、每手都可能触发的严重 leak，影响最大。
2. **把绝对筹码阈值改为 BB 倍数（5.2）**——消除盲注漂移风险，且让参数更好调。
3. **统一突袭大注判定语义（5.3）**——避免正常深筹被误判突袭而过度收紧。
4. **河牌陷阱加 MC 兜底（5.4）**——挽回被硬弃牺牲的价值。
5. **LEAD_LOCK 弹性化（5.5）** 与 **MC 预算合并（5.6）** 作为后续优化。

---

> 附：以上所有阈值与逻辑均来自对 `内容/` 下源码的逐行核对（token 截至 2026-08-22 的版本）。若后续改动，请以 `strategy.py` 顶部常量与 `_face_bet` 等函数实际代码为准。
