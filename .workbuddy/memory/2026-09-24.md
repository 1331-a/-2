# 2026-09-24

## 架构重写：终局效用仲裁（用户指出「不该先决策再改」）
【用户意见】「这里逻辑应该不是先决策再改吧？而是在决策时就要同时考量弃牌的价值，
  而不是决策加注后改成全压」—— 直指旧结构：决策层按底池赔率算出「加注 576」，
  出口补丁 `_doom_call_upgrade` 再把它改写成「全押 19,600」；两个补丁都单向更激进，
  **弃牌的价值从不参与比较**（第51手弃牌其实只损失 400、且未被锁死）。
【新结构】`_win_utility(state, lead_after)`：lead → 最终胜率 U（logistic 平滑，
  锚点 ±2×_blind_line，斜率 UA_SLOPE=1.6）——刻意**不做「线上1线下0」悬崖**，
  因为那两条线是「只靠收盲注」的最坏界，擦线处远不是必败（第51手只越线 112 筹码）。
  `_endgame_eu` 给 fold/check/call/raise/allin 各自算 EU：
  · 加注/全押被跟时 `eq_c = eq^(1+UA_CALL_DAMP×n/底池)`（0.6）——超池全押被跟=已输；
    用**幂次**保证 eq=1（坚果）不被惩罚（乘系数版本实测把四条的价值加注降级成 call）；
  · 对手已全押时 f=0（他没有弃牌分支）——否则「他弃牌我收池」会把领先局面的全押算成划算。
  `_endgame_arbitrate` 的三条护栏：① 只往更保守方向修正（UA_RISK_RANK，不制造新全押）；
  ② 硬性弃牌不被翻案；③ 保留用户硬规则 UA_CROSS_CHECK（09-15 方案B：投进去就越线→过牌）。
【删除】`_doom_call_upgrade` / `_doom_bet_downgrade` / `_doom_exposure_cap` / `_doom_hand_strong`
  （前一版 2-B「便宜跟注封顶」被本架构取代）；`_prepare_globals` 新增 `_LAST_EQ/_LAST_ADJ`。
【实测】第51手 EU：弃牌 0.190 / 跟注 0.286 / 加注576 0.322 / **全押 0.188（最差）**；
  12 次决策 call×9 + raise×3，**0 次全押**；若换对手先 check，则 6/6 = check。
  其余：第35手不再输出 allin；真 doom（已投 17,000）仍由规则2-A 无条件全押。
【验证】test_log_v2 201/201（连续 ~15 次运行稳定）；11 文件 ×3 轮全绿；bundle 行数见提交。

## 规则：防锁赢全押（越线 → 无条件 all-in）+ 端到端链路文档
【用户规则】「越线就全压是合理的」；并澄清 **第51手的问题在「策略顺序」**（那手弃牌
  不会锁赢，所以不该被硬压成全押）——现在的顺序：先判弃牌会不会被锁赢（入口规则2-A）
  → 再判继续投入会不会越线（UA_SEALED_ALLIN）→ 都非硬约束才轮到效用比较。
【实现】`_sealed_by_call(state)`：`lead − 2×敞口 ≤ −2×追回线×(1+UA_SEALED_MARGIN)`；
  在 `_endgame_arbitrate` 里作为**第 ② 条硬约束**（决策层本要 call/raise → `{"act":"allin","lk":1}`，
  且不受 `eq is None` 限制 → 翻前也生效）。`UA_SEALED_MARGIN=0.15`（明显越线才算；设 0 = 只要越线就全押）。
【实战第32手复原（截图）】河牌 2♦4♥3♠9♥K♦ · 我方 9♣J♥（一对 9）· 对手 10♣K♥（一对 K）
  · 决策点：底池 4,217（界面 5,982 是**跟注后**值）/ 需跟 1,765 / 已投 1,226 / 剩余 18,774 /
    lead −3,176（本场 −1,588）/ 剩 38 手（2×追回线 5,700）
  · 弃牌口径 −5,628 > −5,700 → 不 doom（但只余 **72 筹码**）；跟注口径 −9,158 → **越线 3,458 = 61%**
  · → `_sealed_by_call=True` → **allin** ✓（10/10 决策 = allin）；关掉硬规则 → 回落到 call
【第51手对照】越线仅 112 = 3.9% → 擦线不触发 → call（保持用户上次的意见）✓
【新增文档】`内容/端到端策略链.md`：第0层平台 JSON → 第1层 bot.py（_handle_line/_to_response/
  _final_guard）→ 第2层 parse_request 状态推导 → 第3层 decide 优先级主链（规则2 → 规则18 →
  公对/陷阱 → 常规 → 上限守卫 → _normalize → 终局仲裁 ①/②/③ → 规则学习）→ 第4层输出与
  globaldata 持久化，附「一次决策优先级速查表」。
【验证】test_log_v2 209/209（新增 10 条第32手/擦线门槛断言）；11 文件 ×2 轮全绿。

## 平台预检失败排查：`exec /app/bot: resource temporarily unavailable`（无代码改动）
【报错】Bot 预检失败：Bot 进程异常退出 stdout EOF（退出码 255）；stderr 末：
  `exec /app/bot: resource temporarily unavailable` → 平台 shell 的 exec 拿到内核 **EAGAIN**，
  **进程根本没启动**（不是我们的程序崩溃/超时）。
【我们这侧已排除】
  ① 本地冒烟：`内容/botzone_submit.py` 喂一条 request → 正常输出 `{"response":0,...}` ✓
  ② 代码里无 `multiprocessing/threading/subprocess/os.fork/os.exec` → 不依赖平台允许多进程 ✓
  ③ 用户手里的 ELF（`测/学习升级版/poker_bot`，7,413,088 字节，mtime 14:55）：
     `\x7fELF` + 64 位 + **段表末尾正好 = 文件长度**（未截断），且与 09-11 能跑通的那版
     （8676713，7,388,248 字节）**结构完全一致**（段表同样在文件尾、末尾 24 字节同样不是
     PyInstaller cookie、MAGIC 同样只出现在 bootloader 代码里）→ 产物本身没问题。
【产物校验方法（可复用）】读 ELF 头 `e_shoff/e_shentsize/e_shnum`：段表末尾应 ≤ 文件长度
  （本例恰好相等；若 e_shoff 越界 = 文件被截断）。注意 PyInstaller 载荷是 zlib 压缩的，
  **grep 字符串（如 `BOT_VERSION`、代码常量）查不到任何东西，不能据此判定版本**。
【关键发现·下载链路不稳】Downloads 里的 artifact **zip 有多个是空包（22 字节 `PK\x05\x06`，
  0 个文件）**：`poker_bot-linux-x86-64-1f4408f.zip`（今天 14:55）、`...-d465d1d.zip`（09-11）、
  `poker_bot-linux-x86_64(.zip / (1).zip)`（09-10）。→ 上传前必须解压确认里面有 `poker_bot`
  （应为 7,413,088 字节），**不要直接传 zip**。
【EAGAIN 常见成因（沙箱侧）】容器 pids/内存配额被占满、预检环境瞬时资源不足、overlay 懒加载
  首次读取失败 → **首选对策：同一文件重传/换个时间点重试**；并核对上传后的字节数。
【备选构建方案（已向用户提出，待其决定）】① UPX 压缩把 7.4MB 降到 ~3MB；
  ② `--runtime-tmpdir .` 让 onefile 解压到工作目录（避开 /tmp 不可执行）；
  ③ 换 Nuitka --onefile 或 onedir 方案。

## 技术判负定案 + 构建流程加固（commit 288aa4c → 527d14e）
【根因（三层证据闭合）】**PyInstaller 6.22.3（发布 2026-09-12）引入了 onefile 父进程
  安全校验，产物在平台沙箱里秒退** —— workflow 原来用浮动区间 `pyinstaller>=6.10,<7`，
  于是 09-11 解析到 6.22.2（能跑）、09-24 解析到 6.22.3（判负）。
  ① PyPI 上传时间：6.22.0=08-08、6.22.1=08-15、6.22.2=08-17、**6.22.3=09-12**。
  ② 二进制引导器字符串：判负 ELF 比能跑 ELF 多出 `pyi-enable-onefile-parent-verification`、
     `security.capability`、`getxattr`、`/proc/%u/stat`、`originating onefile parent`、
     `_MEI%08x`（两者都有的是 `pyi-runtime-tmpdir`、`PYINSTALLER_STRICT_UNPACK_MODE`）。
  ③ 逐版本扫描 sdist 的 `bootloader/**/*.c|h`：
       6.21.0 / 6.22.0：无 `pyi_security.c`
       6.22.1 / 6.22.2：有 `pyi_security.c`，但**无**父进程校验（旧 ELF 命中 0/43 个 6.22.3 新增字面量）
       **6.22.3**：新增 `pyi_security_verify_onefile_parent_pid` / `..._parent_executable` /
         `..._application_home_dir_name` —— 子进程经 `/proc/<pid>/stat` 追父进程链并比对
         父进程可执行文件路径、解析 `_MEI%08x` 目录名、读 `security.capability` xattr 决定
         是否强制校验；任一步不满足 → `Security validation failure: ...` 并 `return -1`
         （**退出码 255**，与平台报的 255 一致）。
【平台侧表现】stdout EOF、退出码 255、0 手 0 动作 → `technical_loss`。
【改动 build-elf.yml（commit 288aa4c + 527d14e）】
  ① **精确锁 `pyinstaller==6.22.2`**（= 实测在平台上跑过整套对局的那版）并打印版本；
  ② 构建后**冒烟自检**：真跑 ELF，喂 2 条真实格式 request（翻前 + 河牌面对下注），
     断言 stdout 有 2 行合法 `{"response":…}`、stderr 无 Traceback 且出现 `[REQ] hand=`，
     任一不满足即构建失败；
  ③ 兼容性报告：runner uname/glibc/python、`ldd`、ELF 所需**最高 GLIBC 符号版本**；
  ④ **引导器特性守卫**：产物若含 `pyi-enable-onefile-parent-verification` → **构建失败**
     （防日后 pin 再漂到 ≥6.22.3）；
  ⑤ debian:11（glibc 2.31）非阻塞试跑。
【用户输入】对局 log `20260924150103-d5b70f71`：bot_a=测01(我方, seat0) vs cloud(seat1)，
  event_count=4，hand 0 发牌后直接 match_end，reason=technical_loss，winner=1，
  rounds_played=0 → **我们一手都没出**（bot 进程从未应答），与预检失败同因：
  `exec /app/bot: resource temporarily unavailable`（execve 返回 EAGAIN）。
【产物侧已排除】
  · 用户手上 ELF(7,413,088) 完整：段表末端 == 文件长度（非截断）、magic 正常；
  · 与 09-11 能跑通那版(7,388,248) 的 program headers / .interp / 依赖动态库 / shnum
    **完全一致**，只大 24KB；
  · 本地喂 request 正常输出合法 JSON；运行时模块只依赖 json/sys/math/time/itertools/random；
  · 代码内无 fork/subprocess/threading（不额外起进程）。
【重要发现：构建环境漂移】workflow 原本用不锁版本的 `pyinstaller>=6.10,<7`
  → 「同一份代码、不同时间构建，引导器不是同一套」（详见本节开头三层证据）。
【A/B 对照（待用户执行）】已解出 09-11 的 ELF 到
  `C:\Users\HP\Downloads\A_B对照_旧ELF_8676713\poker_bot`（7,388,248 字节）：
  能过预检 → 新构建有问题（pin/瘦身方向）；也失败 → 平台侧配额/策略。
【口径】execve 的 EAGAIN（execve(2)，Linux 3.1 起）＝ set*uid 后超出 RLIMIT_NPROC，
  或装载新镜像时内存/swap 不足 → 属**平台启动侧**，与策略代码无关。
【工具坑】判断 PyInstaller 产物版本不能用 grep（载荷 zlib 压缩），但**引导器区**（ELF
  RX 段，约前 67KB）的 rodata 里有特性字符串，可直接对比；校验完整性用 ELF 头
  e_shoff + e_shentsize*e_shnum 是否越界。

## 工具 / 记忆
- 【口径教训】牌桌界面「底池」是**结果态**（all-in 之后）；复盘必须回到决策前那一帧，
  否则 to_call/底池 比例全错 —— 本轮我最初把 20,592 当决策时底池，得出「对手突袭大注」
  的错误结论，是用户纠正后才修正的。
- 【换算】0-51 牌号：`n = (rank-2)*4 + suit`，suit 0=♠ 1=♥ 2=♦ 3=♣。
  （`cards.py` 里 `rank*4+suit` 的注释是旧口径，别照抄。）
- 【测试稳定性】新增的「端到端动作」断言必须避开 MC 抽样抖动（第51手实测在 call/raise 间
  摆动）→ 端到端只断言「不是 allin」，确定性部分用固定 eq 直测仲裁。
- MEMORY.md 按体积要求重整（16.7KB → 约 8KB）：改为按主题组织（锁赢/doom 数学、
  当前规则清单、牌型与强度、工具陷阱、待办），合并同类项、去掉逐 commit 叙事。
  2026-08 的旧日志暂未删除（用户常拿它们做「当时版本行为」取证），已记为待办。

## 规则20 大额投入闸门（commit dc1bbb8，用户规则）
【用户反馈】截图 UI 第21手（= log hand 20）：河牌 10♠6♦A♣3♥K♦，我方 8♣6♣ = 一对 6
  （最小对子），对手 8♦A♦ = 一对 A；事件 219 对手加注至 6,318（1.14 池）→ 我方全押 17,226。
  用户："只有最小对子为什么还要跟/全押？我记得 >3000 只有 ≥三条能跟，allin 只有防锁赢
  或 ≥三条；即使加入对手策略学习也不应该偏差这么大。"
【筹码复原（与截图完全吻合，本轮定位关键）】底池 11,866 = 双方各投 2,774 + 对手这注 6,318；
  我方 17,226 = 20,000 − 2,774；对手 10,908 = 20,000 − 2,774 − 6,318
  → **本场 0:0**（既非 doom，也不在搏命区；UI 上 cloud「本场 +5,418」不是零和净值，别信）。
【根因链】① 决策层（MC eq 按对手范围）想**跟注** → ② 出口 `UA_SEALED_ALLIN`
  （跟注口径明显越线：−18,184 ≤ −7,400，越线 146%）把它升级成全押。
  ① 之所以成立是**对手模型**把门槛压低/范围估宽：maniac(−0.08) + 规则16(最多 −0.09)
  + 被动型(−0.02) 叠加 → 门槛可到 ~0.20，而一对 6 的 eq ≈ 0.21（中性模型：eq 0.213 / 门槛 0.387）。
  实测 eq 对范围宽度：10%→0.18、35%→0.37、67%→0.49、95%→0.52（`_opp_range_pct` 只看
  「翻前是否加注者 + vpip」，**不看当前街加注行为** → 后续可优化点）。
【修法】新增 `_big_money_guard` + `_strong_for_big_money`（`BIG_CALL_LIMIT=3000`、
  `BIG_MONEY_GUARD_ON`），放 `_decide_impl` 出口**最后一步**（必须在 `_endgame_arbitrate`
  之后，因仲裁升级动作带 `lk` 免检标记）：
    R1 需跟 > 3000 且有效牌型 < 三条 → fold
    R2 **主动**全押只在 [≥三条 / 翻前 AA·KK·QQ·JJ·AKs] 才允许，否则降级
    豁免：应对对手全下（`any_allin` 或 `to_call ≥ my_left`）→ 原样放行（09-14 规则）
    不经过闸门：规则2-A 防锁赢、规则18 搏命区（入口已 return，不被削弱）
【连带影响】第32手（一对 9、越线 61%）不再主动全押 → 降为跟注 1,765（≤3,000，允许），
  该断言已改写。第21手 → fold（放弃 2,774；lead −5,548 仍安全，2×追回线 7,400）。
【验证】test_log_v2 221/221（+12 条规则20 断言）；11 文件 ×3 轮全绿；bundle 6004 行。
【CI】另加「Release 原始附件」下载通道（artifact 的浏览器下载在本机反复产出 22 字节空 zip）。

## v70 vs v50 对局复盘（log 20260924161253，v70 座位0 净输 19,945）
【v50 身份定位方法（可复用）】旧 ELF 无版本戳（09-11 才加）→ ① 用
  `PyInstaller.archive.readers.CArchiveReader(path).extract('poker_bot')` 取出主脚本
  （type 's' 那条，zlib 自动解压）；② 在解出的 pyc 里 grep **特征字符串**（函数名、
  co_varnames 变量名、常量名）做版本指纹；③ 行为回放 `botbattle_log.load_botbattle`
  逐决策点喂历史构建（`git archive <sha> 内容` + 独立子进程避免模块串味）。
  结论：**v50 = 547e4a6（2026-09-07 22:03）**（`flush_suit`/`my_same`/`my_best` 在，
  而 09-10 的 `BetPatternDetector`、`_lock_win_unified` 不在；文件 mtime 22:08 吻合）。
  ⚠️ 回放命中率只差 1~2 点不要当证据（MC 600 次抽样有噪声），变量名指纹更可靠。
【v50 也带对手模型】`archetype` / `_opp_range_pct` / `bet_resp_stats` / `eff_vpip`
  都在 v50 里 —— 用户以为「v50 纯数值、v70 才加对手模型」不准确。v50 缺的是 09-07
  之后的一切：规则12独立模块/权重、规则13、锁赢统一、BetPatternDetector、全下金额
  解析修复、规则2-B/16/17、规则10强化、规则18搏命区、规则19、规则学习、终局仲裁、
  越线全押、规则20。
【这场的钱怎么走的（70 手）】
  · H0–H15 **−3,924**（正常对局亏损：H3 −1,859 / H5 −826 / H15 −2,889）
  · H16–H66 **+3,929**：v70 掉进 doom → **每手开局全押偷盲**（20 手翻前 allin，
    对手全弃，每次 +100/+300），把亏损磨回 ~0
  · **H68 −20,000**（分水岭，全场唯一大额亏损）：v70 4♥5♥ 翻牌一对 4 全押 19,900，
    v50 T♣7♥ 顶对 T 接住并赢
  · H69 −50
【两条触发规则（不是对手模型）】
  1. **H68 = 越线→无条件全押**（`UA_SEALED_ALLIN`）：剩 1 手 → 追回线仅 100 筹码 →
     "跟注即越线"必然成立（越线 290）→ 决策层只想跟 100，被升级成 19,900 全押。
     **今天上线的规则20 已修**（同一请求现在返回 `call`；关掉闸门则复现 allin）。
  2. **H16 起 20 手 doom 全押**：规则2-A「弃牌即被锁死 → 无条件全押」，起点**只擦线
     48 筹码**（lead −7,848 vs 2×追回线 7,900）。这 20 手净赚但尾部风险极大。
【两版量化差异】同一批 88 个决策点：一致 68 / 不同 20（23%）；类型
  raise→allin 11、call→allin 2、fold→allin 2、check→raise 2、raise→call 2、call→fold 1
  → v70 的偏差 15/20 都是「普通动作被变成全押」。
【待用户决定】是否给 doom 也加「越线幅度门槛」（现在越线 1 筹码就整手全押）；
  另一局的 log 未提供。

## 诊断：「和大部分人打前中期劣势巨大」（纯分析，未改代码）
【数据基础】内容/记录/*.json 4 局 + Downloads 2 局 = 6 局 420 手（**其中 5 局是同源自战**，
  双方 display_name 都是「测01」）→ 注意：自战数据无法直接代表「和外部对手」。
【分段结果（6 局汇总）】
  前中期 H1-35：187 手 | 弃牌 63% | 弃牌盈亏 −31,999 | 打到底 70 手 +56,267 | **每手均 +130**
  中期  H36-59：132 手 | 弃牌 69% | 弃牌盈亏 −16,880 | 打到底 41 手 +10,317 | **每手均 −50**
  后期  H60-70： 58 手 | 弃牌 74% | 弃牌盈亏  −4,600 | 打到底 15 手  +2,133 | **每手均 −43**
  逐局看：中期 4/5 局都落在 −1,900±200（高度一致 → 结构性，非运气）；
  后期单局 ±19,400~20,000 巨幅波动（全押规则主导，胜负基本在这一段定）。
【盈亏结构的致命点：97.6% 靠弃牌收场，全部利润来自 2.4% 摊牌】
  赢：对手弃牌 157 手 均 +261；摊牌 7 手 均 +10,242
  输：我方弃牌 251 手 均 −213；摊牌 3 手 均 −13,953
  → **弃牌战场净 −12,446**；若无那 7 手摊牌（+71,693）则整体为负。
  → 对「不爱弃牌」的对手：盈利端（靠对手弃牌）消失、亏损端照旧 = 前中期大亏。
【手级模式（420 手）】
  ①翻前直接弃     180 手 → −13,650（均 −76，= 交盲注磨损）
  ②进场后翻后弃    71 手 → **−39,829（均 −561）** ← 最大单项死钱
  ③打到摊牌/对手弃 169 手 → +70,867（均 +419）
  ②的解剖：牌型 高牌 47/71（66%）、一对 18；街 flop 46（65%）；
    命中规则：无坚果/强听 64/71（90%）、**突袭大注(4倍) 27（38%）**、大注(>0.6池) 23（32%）
【分段弃牌的翻前首动作】中期 132 手里 **70 手（53%）翻前直接弃牌**（弃牌率随局数递增 63%→69%→74%）
【翻前尺寸】我方翻前加注 n=143：中位 300、75% 分位 300、最大 850；87% ≤300。
  常量：OPEN_SIZE_BB=2.5 / STEAL_OPEN_BB=2.5 / OPEN_SIZE_VS_STATION=2.2 / ISO_SIZE_BB=3.2
  → 200BB 深度下 2.5-3BB 开池几乎无隔离效果（对手已投 100，只需再补 200 就跟）。
【自适应实测失效】同一场景（顶对 vs 70% 池）把对手换成 中性/跟注站/疯子/岩石 →
  decide 全部返回 fold，**动作完全一致**（0.6/突袭等硬规则在赔率与画像之前 return）。
  前 24 手更彻底：规则16 的 h_part 需剩 ≤45 手才 >0；规则19/规则学习需 ≥4 样本。
【反事实矩阵（牌号校验后，公面 J♠7♥3♦）】
  底池 200（翻前各 100）：AA/QJ/88/33 → 任何尺寸(30%-150%池) 都 call/raise ✓ 正常；
    AQ 高牌/T9 卡顺 → ≤30%池 call，≥50% 弃（合理）。
  底池 800：同上前提，AA 在 150% 池转 call。
  → **「to_call > 0.6×pot」这条其实是 1.5 倍超池才触发**（state.pot 含对手本次下注）
    —— 所以它不是「60% 池就弃」的悬崖，我最初的口径错了。
  → 但「突袭大注」用的是 **相对对手本手此前投入的 4 倍**：小底池里对手翻前只投 100、
    翻后下 500 → 5 倍 → 直接弃（<三条）。实测：AA/QJ/88/AQ/T9 全部 fold，只 33 三条 raise。
    这是**对「翻后激进/尺寸不懂」的对手的真实弱点**。
【7 手摊牌均 +10,242 / 3 手均 −13,953】→ 打到底时并不差，问题是**打到底的手太少**（2.4%）。
【结论（待用户给外部对手 log 验证）】
  前中期劣势 = ① 靠对手弃牌赚小钱（均 +261）但漏损更大（弃牌均 −213~561），
  ② 中期起弃牌率 69-74%（含 53% 翻前直接弃）持续交盲注+死钱，
  ③ 前 24 手自适应失效 → 固定模板，对任何对手同一套打法，
  ④ 后期有 doom/搏命/全押能强制摊牌翻盘，前中期没有 → 只能被慢慢磨。
【诊断方法（可复用）】① botbattle_log.load_botbattle 逐决策点重放 + 当前代码 decide 归因；
  ② 按「手」分类：翻前首动作 × 最后动作 → ①翻前弃 / ②进场后弃 / ③打到底；
  ③ 赢/输手的 settle.reason 分布（看是否依赖对手弃牌）；
  ④ 反事实矩阵：固定公面+底池，扫牌型 × 对手下注尺寸。

## 【修正·关键】vs 外部对手 cloud 的 3 局实证（推翻了自战结论）
【数据】Downloads 4 局中 3 局是 **测01(amc,id1186) vs cloud(id1139)**（外部对手）：
  154120 → **−17,459（输）** | 160802 → **+18,490（赢）** | 160909 → **+16,971（赢）**；
  161253 是自战（−19,945）。我方都在座位0。
【分段（决定性的）】胜负 **100% 由前中期决定**：
  154120: 前中期 **−20,059** | 中期 +? | 后期 +750 | 全场 −17,459
  160802: 前中期 **+19,790** | 中期 −1,000 | 后期 −300 | 全场 +18,490
  160909: 前中期 **+18,421** | 中期 −1,000 | 后期 −450 | 全场 +16,971
  → 对比自战（前期 +小、中期 −1,900、后期 ±20,000）**完全相反**：vs 外部对手时
    前中期就是全部，后期几乎不动 → 用户「前中期劣势巨大」的体感**成立**。
【机制：前中期的「一次 20,000 全押对决」+ 前 20 手小亏推高 doom 概率】
  3 局的关键手都是 ~20,000 全押：154120 H21 **输**（我 8♣6♣=一对6 / 对手 8♦A♦=一对A）；
  160802 H19 **赢**（我 K♣K♦）；160909 H7 **赢**（我 A♠8♠）。→ 2 胜 1 负 = 运气主导。
【输局的精确因果链（H21，即用户 15:41 截图那手）】
  前 20 手小亏 → H21 开始时 lead 仅 −2,218（单边落后 1,109）
  H21: 翻前 ra300→call200 | 翻牌 对手565→我call | 转牌 对手1909→我call | 河牌 对手6318→**我 allin 17226 → 输 −20,000**
  口径核算（剩 49 手，2×追回线 7,300）：
    · 翻牌弃 → 已投 300 → −2,218−600 = −2,818 **安全**
    · 转牌弃 → 已投 865 → −2,218−1730 = −3,948 **安全**
    · 河牌弃 → 已投 2,774 → −2,218−5,548 = **−7,766 ≤ −7,300 → doom（擦线仅 466 = 6.4%）**
  → 当前代码重放：翻牌 call、转牌 call（注意 **doom(跟注口径)在转牌就 True**）、
    河牌 **allin**（doom 弃牌口径成立 → 规则2-A 无条件全押）。
  → **结论：只要在翻牌或转牌弃牌就不会 doom；是「连续跟注把已投推高到 2,774」把自己逼进死角。**
    前 20 手少亏 500 就能避免（500 > 擦线 466）。
【前 20 手小亏的共同模式（3-4 手，每手 −700~−1,210）】
  H9:  我ra300 → 翻牌 对手519 我call → 转牌 对手1316 我fold → **−819**
  H11: 我ra300 → 翻牌 我ra222(对手check后小注,被跟) → 转牌 我ra431 → **−953**
  H4:  对手ra260 我call → 翻牌 对手171 我call → 转牌 对手284 我call → **−715**
  160802 H1: 我ra300 → 翻牌 对手455 **我ra910** → 转牌 对手2192 我fold → **−1,210**
  160909 H1: 我ra300 → 翻牌 我ra188 → 转牌 我ra441 → **−929**
  → 即「翻前 ra300 + 翻后小注(188-222)或跟注(519-565) → 转牌被更大注(1316-2192)赶走」。
【对手 cloud 风格】加注尺寸中位 **620~1316**（最大 17,226~20,000），我方中位 **300**。
  cloud 是我们尺寸的 2-4 倍；我们用小注+便宜跟注，被对手"递增下注"逐步榨取。
【手级模式（vs cloud 3 局 136 手）】①翻前就弃 92 手 −6,100（均 −66）；
  ②进场后弃 **仅 7 手** −5,226（均 −747）；③打到底/对手弃 37 手 **+25,628（均 +693）**。
  → 与自战完全不同的画像：vs cloud 时我们打到底的比例高得多（37/136 = 27% vs 自战 2.4%）。
【输局的次生现象】154120 我方 **allin 25 次**（H21 输光后 49 手每手全押偷盲），
  而赢的两局 allin 仅 1 次 → "落后→全押偷盲"是输的结果、不是原因。
【我方加注尺寸常量】OPEN_SIZE_BB=2.5 / STEAL_OPEN_BB=2.5 / OPEN_SIZE_VS_STATION=2.2
  → 实测翻前加注中位 300（87% ≤300）；**深筹码下毫无隔离效果**。
【待用户决定（本轮未改代码）】
  A 前 25 手启用早期自适应（降规则16/19/学习的样本门槛）
  B 突袭判定改为相对底池（`≥4 倍 且 绝对额 ≥ 底池`）
  C 翻前开池加大（2.5BB → 3~3.5BB）
  D 提高"翻牌跟注"门槛（跟注会引出更大注 → 应视同承诺后续投入）
  E doom 擦线（如 6.4%）是否仍强制全押 —— 用户上轮说"越线就全压合理"，
    但本例擦线 466 就全押、代价 20,000；关键在于**前中期不该让自己落到越线位置**。


================================================================================
【2026-09-24 · 方案A 实施与验收（future_cost 计入跟注门槛）】
================================================================================
用户指令：先做 A（_face_bet 加 future_cost）→ 观察 5-10 手看前 20 手累积亏损是否降低
→ 不够再做 B（OPEN_SIZE_BB=3.5）→ C 并入 A → D 不做 → E 不动。

■ 已实现（strategy.py，唯一生产文件）
  · 常量：FUTURE_COST_ON / _STREETS / _POT_FRAC(0.75) / _DISCOUNT(0.45) /
    _WEIGHT(0.50) / _GAIN(0.50) / _STACK_FRAC(0.25) / _CHEAP_MAX(1500) /
    _CAP_ON / _CAP_MIN_HANDS(40) / _CAP_MIN_RATIO(0.35)；WB_FC_DEBUG 调试口。
  · 函数：_future_cost / _cheap_call_cap_hit / _cheap_call_guard / _lock_win_engaged。
  · 出口顺序：..._normalize → _endgame_arbitrate → _big_money_guard → _cheap_call_guard。
  · A/B 开关：环境变量 WB_NO_FUTURE_COST=1 → FUTURE_COST_ON=False（只读对比用）。

■ 两处关键修正（都踩过坑）
  ①【口径修正】_future_cost 早期实现 `per×streets` 不折现、不封顶总投入 →
    翻牌跟 0.28×池的门槛从 eff_req 0.219 抬到 **0.640（3 倍）** = "非坚果即弃"。
     改为「逐街折现 disc^(i+1) + 总投入 ≤ 0.25×有效筹码封顶 + 代价/收益各按
     0.5 权重双向计入」→ 典型抬升收敛到 **+0.03~+0.08**（实测 11 个折算点）。
  ②【便宜判定口径修正】_cheap_call_cap_hit 早期用「to_call 是否远大于本手已投」
     做便宜判定 → 方向错。第32手（inv=1226、to_call=1765、pot=4217）明明是
     **大额跟注**，却被 `inv+to_call > cap` 判成便宜跟注 → 误弃掉防锁赢全押，
     test_log_v2 出现 FAIL。改为 **相对底池**：`to_call ≥ 0.35×pot` → 算大注，豁免。
     ✅ 修正后 test_log_v2 = 221/221，11 个测试文件全 0 FAIL。

■ 验收结论（诚实、未见预期的正向效果）★
  · 真实日志 A/B（diff_ab.py，子进程隔离）：4 局共改 5 点（~2-6%）；
    154120 败局只改 raise 尺寸抖动（H3/H7/H11/H21），**H9/H21 的 call 不再翻成 fold**。
  · 早期「A 让 H9/H21 翻弃」是**过激公式**的产物：那版把门槛从 0.317 抬到 0.363，
    但 H9/H21 对随机牌的 MC 胜率是 **0.62~0.66**，只有 +0.28 的抬升才可能翻 ——
    而 +0.28 = 不现实的惩罚。**A 在合理幅度下无法改变这两个旗舰案例。**
  · H21 输光 20,000 的真因**不是翻牌跟注**：河牌 `_lock_win_unified → allin`（规则2-A
    doomed 无条件全押，lk 免检，入口 return，绕过规则20）。即 H21 是规则2-A 的
    既定设计行为，future_cost 管不到。
  · 唯一行为级改动是 161253 的 H69 flop `fold → call`（**变得更激进**，非预期方向），
    因 required 变化跨过了 draw 分支边界。
  · 本地配对模拟 16 组×70 手：13/16 组完全无变化；变化组的「前20手累积」差异
    −3753/-1550/+350 混合，均值 −309（**略差**）。
  → **判定：A 的实测效果不足以支撑用户"前20手亏损降低"的验收标准，且幅度调大即有害。**
    建议转 B（OPEN_SIZE_BB 2.5→3.5 + 联动），因为根因是「我方 300 vs 对手 620~1316
    的尺寸差 + 小注便宜跟注链」，而 future_cost 只能微调跟注门槛、动不了下注尺寸。

■ 新增工具（内容/）
  · probe_lift.py —— 用真实日志统计 future_cost 触发率/抬升分布（快，推荐）。
  · baseline_early.py / diff_ab.py / ab_effect.py / replay_early.py —— 验收集。
  · 【坑】simulate.py 是旧接口（GameState(kwargs) 已改为 GameState(request)），
    不可用；本地对局模拟需自建引擎（本轮临时写的 eval_early_ab.py 已删）。
  · 【坑】decide() 会就地 mutate model/ctx → 同一 state 重复调用结果会漂移；
    必须每次用**全新进程**或新 model/ctx，否则得到假的 A/B 差异（本轮踩过）。

■ 代码现状：A 保持 ENABLED（FUTURE_COST_ON=True / FUTURE_COST_CAP_ON=True），
  幅度为「温和版」。已全量回归通过，但**未推送 GitHub**（等用户对 A 去留的决定）。
