# 2026-09-24

## 规则2-B：敞口 doom 下「便宜跟注」不再升级全押（用户规则，实战第51手）
【用户反馈】截图「第51手」河牌：我方（座位2）A♦4♣ = 一对 A，对手 6♣9♥ = 两对；
  我方 check → 对手下注 192 → 我方 all-in 19,600 → 被跟 → 输掉整场。
  用户纠正：界面「底池 20,592」是 **all-in 之后**的显示值（992 + 19,600），决策时只有 992。
【真实口径（log hand=50，界面 1-based）】已投 400 / 需跟 192 / 剩余 19,600 / 落后 914 /
  剩 19 手（追回线 1450）：
  · 弃牌口径 −1828 − 2×400 = −2628 > −2900 → 不 doom（弃牌安全）
  · 跟注口径 −1828 − 2×592 = −3012 ≤ −2900 → doom（**余量仅 112 筹码**）
  → `_doom_call_upgrade` 把决策层的「加注 576」升成 19,600 全押 ← 根因
【修法】新增 `_doom_exposure_cap` + `_doom_hand_strong`：敞口 doom 成立时
  · to_call ≤ DOOM_CAP_STACK_FRAC(0.15) × 我方剩余 且手牌不强 → **call**（封顶）
  · 强牌（翻后≥两对 / 翻前超强）/ 跟注本身重投入 / 跟即全下 → allin
  · 规则2-A（弃牌口径 doom）**不做**豁免，仍无条件 allin（弃牌=认输）
【副作用（已在答复中告知用户）】09-15「过不了牌就 allin」被收紧：第9手（跟 815/18279=4.3%）
  与第35手（跟 300/17900=1.7%）现在都是 **call** 而非 allin；恢复旧行为把
  `DOOM_CAP_STACK_FRAC` 设为 0 即可。
【验证】test_log_v2 198/198（新增 13 条：底池 992 / 需跟 192、两口径 doom、封顶 call、
  端到端 call、加注也被封顶、关掉便宜线复现 allin、两对仍 allin、重投入仍 allin、
  真 doom 无豁免）；11 文件 ×3 轮全绿；bundle 5820 行。

## 工具 / 记忆
- 【口径教训】牌桌界面「底池」是**结果态**（all-in 之后）；复盘必须回到决策前那一帧，
  否则 to_call/底池 比例全错 —— 本轮我最初把 20,592 当决策时底池，得出「对手突袭大注」
  的错误结论，是用户纠正后才修正的。
- 【换算】0-51 牌号：`n = (rank-2)*4 + suit`，suit 0=♠ 1=♥ 2=♦ 3=♣。
  （`cards.py` 里 `rank*4+suit` 的注释是旧口径，别照抄。）
- MEMORY.md 按体积要求重整（16.7KB → 约 6KB）：改为按主题组织（锁赢/doom 数学、
  当前规则清单 1~11、牌型与强度、工具陷阱、待办），合并同类项、去掉逐 commit 叙事。
  2026-08 的旧日志暂未删除（用户常拿它们做「当时版本行为」取证），已记为待办。
