# 2026-08-28 工作日志

## 锁胜/doom 公式精确化收尾（commit c6ddf01，2026-08-27 晚提交）
用户方案："修正本局已投入筹码未被计入的误差，精度提升至无偏"（第一层数学硬约束）。

### 评估结论（重要）
- **fold_out 修正（用户公式正确）**：`lead > _blind_line(剩余局数) + 本局已投入(invested)`
  —— 本局弃牌除未来盲注损失外还有已投入；深投入时不再过早锁胜弃牌。
- **doom 修正（用户公式有数学错误，已修正）**：
  - 用户原式 `lead + 追平线 + invested ≤ 2×BB`：invested 放错边（左边），
    投入越大越难触发（方向反了）；且本局损失用常数 2×BB 而非实际投入。
  - 正确式：`lead - 2×invested ≤ -追平线`（即 lead + 追平线 ≤ 2×invested）——
    本局失败我方净损 invested（我-invested/对手+invested → lead 降 2×invested）。
- invested = INIT_CHIPS - my_chips（含盲注+跟注/加注）。

### 行为变化（用户已确认 A 版）
- 深投入局 = 生死局：任何 invested > (lead+追平线)/2 即确定锁死 → 无条件 allin。
- 约 15 个测试场景参数调整（浅投入隔离 doom 或接受 allin；如 allin_river_req
  默认翻前 3000→500、deep_pf 3000→500、test_lead_lock my_chips 15000→18500 等）。
- **规则3（BET_CAP=3000 降级）在深 pot 场景被 doom 覆盖**（st10 改为验证 doom 接管）。

### 验证与发布
- 全量回归 ×3 稳定；bundle 3400 行；冒烟：深投入不锁胜弃牌→doom allin（A 版预期）。
- 推送：直连恢复（无需代理）bc54f58..c6ddf01；Actions c6ddf01 completed success ✅。
- 注：根目录 strategy.py（untracked 查看副本）与 测/poker_bot（用户自换 ELF）未纳入提交。

## 2倍系数修复（commit 232592a，用户反馈驱动）
【用户反馈】领先(+2000后)爱弃牌拖回均势、好牌不下注。
【根因（重大 bug）】lead（我-对手累计净赢差）变化是筹码损失的 **2 倍**
（每局弃牌我-X/对手+X→差-2X）；`_blind_line` 只返回筹码损失（SB/BB），
c6ddf01 精确公式化时直接当 lead 阈值用，少算 2 倍：
- fold_out 剩余1局 lead=60 误触发→弃牌后 -40 直接输掉；
- 旧极值公式 1.5×BB≈2×平均盲注（系数已含），精确化时丢失。
【修复】fold_out: `lead > 2×(盲注线+invested)`；
doom: `lead - 2×invested <= -2×追回线`。
【验证】冒烟：剩余1局lead60→正常打牌；领先2500剩20局AA→正常下注456；
末期领先4000剩5局→锁胜弃牌保留。全量回归×3稳定。
【附带】st10 深投入大底池回归规则3降级（×2 后追回线 9750>8000 不锁死）。
推送 c6ddf01..232592a；Actions 232592a completed success ✅。

## 2026-08-29 AK 误弃修复（commit c875096，用户截图驱动）
【用户反馈】第43手领先时拿 AK 翻牌前弃牌（截图：我方 19950/投50、对手 100、底池150）。
【根因】game_state.py 翻前盲注动态推导（85-93行）：请求中 my_chips 与实际位置不一致
（BB 位只扣 50）时推导出 SB=25/BB=50（减半）→ _blind_line 减半（2000→1000）→
fold_out 线塌陷（4200→2100）→ lead 2478 误触发锁胜弃牌 → AK 被弃。
【修复】推导加平台下限钳制：SB≥max(推导,50)、BB≥max(推导,100)，盲注不再减半。
【验证】截图场景复现→修复后 AK 3-bet(300)；全量回归×3；冒烟 BB位300/SB位250。
【LEAD_LOCK 按用户要求未改】已撤回 08-28 晚的全部 LEAD_LOCK 修改（强牌跟全下方案
不采用，保留"对手全下必弃"原行为）。测试同步还原。
推送 232592a..c875096；Actions c875096 completed success ✅。
