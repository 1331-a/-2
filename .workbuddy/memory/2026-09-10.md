
## 2026-09-10 第62手弃牌锁赢 triage(用户截图)
【用户】我方红方弃牌"对手锁赢了"，算式 645-150*4=45
【解读】对手领先(我方twc=-645)，剩余4手，追回线=150*4=600(含2×系数)
  → 追不上 → 落后方 doomed
【复核结论】当前代码(06f4269) 4 种构造(66/70手 × ±645/±1290) 全部返回 allin
  (doom→无条件allin，不弃牌) → 截图弃牌是旧 ELF
【公式核对】追回线=2×_blind_line(own=False)=2×(2×100+2×50)=600
  与用户 150×4=600 一致; lead 1290(单边645×2) > 600 → doomed ✓ 无误判
【待用户】重下最新 ELF 重新上传(前几轮同因: 截图都是旧 ELF 行为)

## 2026-09-11 根因修复：平台 hand 是 0-based → hands_left 多算一手(commit 4356628)
【用户反馈】① 每次跑的都是最新版 ② 平台已切换到 botarena
  ③ 给出 P0/P1/P2 代码审查清单
【根因（数据驱动证实，非推测）】平台 request.hand 是 **0-based**(0..max_hand-1)，
  日志/回放界面显示为「第 hand+1 手」。而 `hands_left = max_hand - hand_num`
  把「本手」也算进剩余手数 → **多算一手** → 锁赢线虚高 2×每手盲注≈300 →
  该锁胜弃牌时不敢弃（继续加注）、该认 doomed 时不 allin = 用户看到的
  「锁赢了还在加注」。
  证据：对局日志重放把 hand 回退 1（=真实 0-based 值）→ 锁赢点动作不符
        从 3~4 处降到 **0 处**（两局交叉验证）；
        第19手 lead=7836：hands_left=52(含本手)→阈值7900→不锁→加注(实际)
                          hands_left=51(本手之后)→阈值7800→锁→弃牌(应然)
【修复】新增 `_hands_left(state)=max(0, max_hand-1-hand_num)`（兼容 1-based：
  hand_num>=max_hand→0），替换全部 8 处计算点。
【用户 P0】P0-1 锁赢 fold 在 to_call==0 非法→check；P0-2 锁赢分支过 _normalize；
  P0-3 decide 入口硬性锁赢拦截（_lock_win_legal + _normalize 直接返回）
【用户 P1】P1-1 加测试(_hands_left 0/18/69、_blind_line(70)=5250)；
  P1-2 锁赢日志 record=True 带 cands → 进规则健康度；
  P1-3 _decide_impl 异常兜底(_safe_fallback_action)
【用户 P2】P2-1 [BOT_VERSION]+__file__，CI 把短 SHA 写进 BOT_VERSION 且产物名带
  hash(poker_bot-linux-x86-64-<sha>)→版本可自证；P2-2 [GUARD] 日志(安全网改动作即播报，
  实测主循环只经 _to_response+_final_guard，不改决策)；P2-3 [REQ] 每手请求字段自检
  (hand/max_hand/twc/cards/hist)——换平台后一眼看出协议变化
【工具】botbattle_log 发原始 0-based hand(meta.hand 仍 1-based 供显示)；
  view_log/audit_lock 显示 1-based；新增 check_elf.py ELF 一致性自检(锁赢点硬校验)
【验证】3 次全量回归(11 文件)稳定；bundle 5038 行；推送 06f4269..4356628

## 2026-09-11 二轮审查 P0/M1/M2/M3/D2 五项修复(commit d379596, 用户审查清单)
【复核结论】5 项全部属实（含 1 个真 P0）
【P0】decide 入口拦截早于 _decide_impl → _lock_win_unified 读到**过期**的
  模块级 _LEAD_LOCK → 本应「领先>2000 不 allin」的局面可能走 profit_lock 全下
  → 新增 _is_lead_lock(state) 现算；_lock_win_unified 内部改用它；
    decide 入口同时刷新 _LEAD_LOCK（下游 _allin_floor_guard 复用）
【M1】_stability_mode 阈值少了 2×：lead 本身是筹码差的 2 倍，原
  `lead >= 0.8×_blind_line` ≡ 「40% 锁赢线就求稳」→ 过度求稳
  → 新增 _lock_line(state)=2×(盲注线+本手已投)，改 `lead >= 0.8×line`
  实测(120 决策点)：条件2 命中 75 → 36 次（砍 39 次误判）
  【注意】条件1（对手 allin≥20%）才是主力（本局 57 次）——两条件 OR
【M2】_lock_win_unified 重复调用 → _decide_impl 新增 _lw 参数(_LW_UNSET 哨兵)
  复用入口结果，同时消除两次调用可能不一致的隐患
【M3】_safe_fallback_action 在 doomed 下返回 fold(=认输) → 先判 doomed→allin
【D2】bot._final_guard「弃牌永远合法」不成立：to_call==0 时必须过牌；
  返回 -1 被平台拒收后可能 fallback 成加注 → 改为 -1 if to_call>0 else 0
【DRY】_fold_out_active / explain / _stability_mode 统一用 _lock_line()
  （锁赢线公式曾三处分叉）
【测试】test_log_v2 54 条断言(新增五组)；3 次全量回归稳定；bundle 5096 行
推送 4356628..d379596

## 2026-09-11 M4 修复：decide 入口统一刷新模块级全局(commit d465d1d, 用户审查)
【问题(复核属实，且不止 _CTX)】decide() 入口锁赢拦截早于 _decide_impl()，
  而 _CTX/_OPP_JUMPED/_OPP_BETS_PER_HAND/_MODEL_REF/_DECISION_STARTED_AT
  原本只在 _decide_impl 里赋值 → 入口读到**上一手旧值**：
  · _CTX 过期 → _match_adjust 的 doomed 偏移用旧档位（差 2×LEVEL_SHIFT_BB=12BB）
  · _OPP_JUMPED 过期 → 对手突袭大标滞后一手
  · _OPP_BETS_PER_HAND 过期 → 跟注门槛微调滞后
  · _DECISION_STARTED_AT 未复位 → 超时判断基于上一手起点
【修复】抽出 _prepare_globals(state, model, ctx, reset_clock=True)：
  decide 入口 与 _decide_impl 共用同一份实现；_decide_impl 加 _prepared 参数
  （入口刷过则不重复计算/不复位时钟）。
【复现】同一状态 保守档→normal(不allin) / 激进档→doomed→allin；
  全局留保守档旧值+本手传激进档 → 修复后正确 allin
【测试】test_log_v2 61 条断言(新增 M4 六项)；3 次回归稳定；bundle 5113 行
推送 d379596..d465d1d

## 2026-09-11 生成《代码流程说明.md》(面向初学者的全流程文档)
【用户要求】独立读懂代码：从平台输出→代码运行→返回的完整流程，初学者也能看懂，
  部分函数调用要提供解释说明
【产出】项目根目录 代码流程说明.md（约 500 行 Markdown）
【结构】
  1 整体地图(平台↔bot.py↔各模块) + 2 十步时序 + 3 bot.py(门卫:run/_handle_line/
  _extract_request/_to_response/_final_guard) + 4 game_state(协议字段表/座位归一化/
  _analyze 状态重建) + 5 opponent(统计/贝叶斯收缩/archetype/增量更新) +
  6 match_ctx(赛制档位) + 7 strategy(decide 入口/优先级链/锁赢公式/翻后输入/日志) +
  8 合法性双保险(_normalize + _final_guard) + 9 完整实例走查 + 10 打包部署流水线 +
  11 函数速查表 + 附录 Python 概念小贴士(8 个折叠块:dict/list/推导式/try-except/
  函数/property/global/类与实例)
【准确性】文中示例(第47手 hand=46 lead=3632 to_call=0)用真实代码跑过，
  锁赢线3600<领先差3632 → check → 0，与文档完全一致
