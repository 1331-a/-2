# 2026-09-12

## 生成《程序运行逻辑图》(4 张函数级流程图 + HTML 离线版)
【用户要求】画图清晰表示程序运行逻辑，精确到每个函数及其调用方法
【产出】内容/程序运行逻辑图.html（自包含，含 4 张 SVG + 文字版调用清单 + 函数速查表）
  四张图：
    1. 主流程：平台 → run() → _handle_line() → parse_request/建模 → decide → _to_response
       → _final_guard → stdout，含每步数据形态与 globaldata 回环
    2. 准备阶段调用树：_handle_line 下三列（① 解包：_extract_request/_persisted_str/
       parse_request/_fallback_resp；② 状态重建：_analyze/座位归一化/to_call,pot,stage/
       my_left,any_allin；③ 建模：OpponentModel.from_json/build_model_from_history/
       MatchContext.from_dict/ctx.update,sync_baseline）
    3. decide 决策主链（自上而下，命中即返回）：_prepare_globals → _lock_win_unified
       (A/B/C) → 命中返回 | 未命中 → _decide_impl → _lock_win_tail_guard → _bet_cap_guard
       → _aggressive_strong_bet → _normalize；旁路：except → _safe_fallback_action
    4. 锁赢公式链：GameState → _hands_left / _blind_line(own) / lead → _lock_line
       → _fold_out_active / _profit_lock_allin / _match_adjust → _lock_win_unified
【核对】所有函数名与调用方式均 grep 自当前源码（strategy.py 各函数行号已确认：
  _allin_floor_guard:261 _is_lead_lock:316 _lock_line:331 _lock_win_unified:346
  _profit_lock_allin:386 _bet_cap_guard:481 _lock_win_tail_guard:518 _aggressive_strong_bet:535
  _prepare_globals:572 _hands_left:980 _blind_line:1004 _fold_out_active:1042 _match_adjust:1079
  _preflop_decide:1212 _check_bet_weight:1503 _opp_check_bet:1546 explain:1862 _candidate_rules:1960
  _winning_rule:2027 _postflop_decide:2279 _stability_mode:2367 _check_side_stable:2396
  _check_side:2412 _face_bet:2600 _last_hand_no_fold:2846 _lock_win_legal:2887
  _safe_fallback_action:2897 _normalize:3001；bot.py: _to_response:45 _final_guard:57
  _fallback_resp:99 _extract_request:113 _persisted_str:128 _handle_line:139 run:209）
【未提交】HTML 为新文档文件（未 git add）
