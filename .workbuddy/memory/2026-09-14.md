
## 2026-09-14 规则2扩展: 禁止「只投入不搏」(commit 65aab9f, 用户截图第35手河牌)
【用户截图】第35手河牌: 对手加注至300, bot 只跟注300 → 输掉后对手锁赢
  用户规则: 「跟注输了也会导致对手锁赢」→ 所有同类型情况当手无条件 allin
【数据闭环验证】截图显示「本场 -2,881」= 结算后值;
  决策时 = -2881 + 2400(本手亏损) = 单边 -481 → lead = -962 ✓
  已投入 2100(转牌前双方各投2100) / to_call 300 / 敞口 2400
【根因】doom 只用 invested = INIT_CHIPS - my_chips(**已投入**):
    弃牌口径 -962-2×2100 = -5162 > -5200 → 不 doomed → bot 只跟注
    跟注口径 -962-2×2400 = -5762 ≤ -5200 → 跟注即送掉比赛 (漏判)
【第一版做错(被回归抓出)→ 必须收窄】直接把 to_call 塞进 _match_adjust:
  让「弃牌本来很安全」的牌也判 doomed → 8 条既有断言失败
  (均势翻前面对大额全下应弃 / 72o面对1500应弃 / 规则1大注弃牌 / TT均势
   跟全下应弃 / ICM压力 / KJo面对5300突袭应弃… 全被强改 allin)
  本质: to_call 是**尚未投入**的钱, 弃牌即可避免; 只有已投入是沉没成本
  → 两个口径必须分开
【实现】
  _invested(state)  = INIT_CHIPS - my_chips  (弃牌视角损失)
  _exposure(state)  = _invested + max(0, to_call)  (跟注视角敞口)
  _doom_risk(state, lead=None, hands_left=None, include_to_call=False)
    同一不等式单点实现, 开关切换口径
  _doom_call_upgrade(state, action): include_to_call=True 成立时
    call/raise → allin(fold/check 不动); 条件 to_call>0, my_left>0, 非 _LEAD_LOCK
  _decide_impl 出口在 _normalize **之后**调用 —— 否则会被 _bet_limit 降级
    (非≥三条 且非 doomed 时 allin 被拦; _bet_limit 对 doomed 返回无限大)
  explain() 锁赢面板补 exposure / to_call
【关键常量关系】_bet_limit(state) 对 doomed 返回 2^60(无限) → 这是为什么
  收窄后 allin 能通过合法性校验(doomed 不在, 但升级发生在 normalize 之后)
【测试框架真 bug 修复】test_log_v2.py 汇总输出原在 L296, 后面 0911/0914
  两批 21 条断言在汇总**之后**执行 → 失败被静默丢弃
  已把汇总块移到文件末尾: 61 → 83 条断言全部真实生效
【验证】3 次全量回归(11 文件)稳定; test_log_v2 83/83
  影响面(3 局真实日志): allin 数 0→0 / 9→10 / 2→2 (仅 1 处 raise→allin)
【工具/构建】bundle 4946 行; 推送 65aab9f

## 2026-09-14 规则14: 我过牌后对手小注→假定诈唬 (commit 7b9ef82, 用户规则)
【用户规则】自己过牌后对手立刻小额加注(默认<300, 激进派<500, 具体值受风格
  影响) → 假设诈唬 → 可尝试跟注或反加小注(不弃牌)
【实现】
  常量 BLUFF_READ_BET=300 / BLUFF_READ_BET_AGGRO=500 / BLUFF_READ_AGGRO_AF=0.52
       BLUFF_READ_RAISE_FREQ=0.35 / BLUFF_READ_RAISE_MULT=3.0
  _my_checked_this_round(state): 与 _opp_checked_this_round 对称(扫本街
    history, 兼容 check/call0/无type 三编码 + 1-based 座位 id_base)
  _bluff_read_threshold(model): maniac 原型 或 原始 bet_freq≥0.52 → 500, 否则 300
    (用原始 bet_freq 而非 eff_*: 后者向先验0.40收缩, 小样本到不了阈值;
     与规则8 BBP 口径一致。实测 bet_freq=0.73 阈值→500 ✓)
  _small_bet_bluff_read(state, model, category, strong):
    条件: 翻后 + 本轮我方已过牌 + to_call ≤ 阈值 + 非 any_allin + 非 strong
    动作: 35% 反加(3× 对手本轮注额, 同项目既有 3.0× 口径) / 65% 跟注
  【接入点】_face_bet 开头, **排在「大注(>0.6池)弃牌」之前** —— 否则小底池
    里的小注会被那条规则当大注丢掉(正是用户反馈的场景)
【边界(已实测)】
  - 翻前不触发(小额是常规加注, 无"小额诈唬"含义)
  - 对手全押不触发(any_allin)
  - 我方未过牌不触发(对手先下注的 donk 不算)
  - 强牌 strong=True 不被接管(交回常规价值路线; 固定3× 不如价值尺度)
  - 超阈值交回原逻辑(默认400 → 不触发; 同一400对激进对手 → 触发 ✓)
  - 【踩坑】测试用 {'action_type':'call','action':0} 想表达"跟注过" — 但
    call 0 本身就是 check 的等价编码 → 被正确判为过牌。构造"真·跟注"需
    action>0(如 opp bet 200, 我 call 200)
【影响面(3局真实日志)】规则14 命中 4/5/6 次; 动作变化方向以 fold→call/raise
  为主(少数 diff 为既有尺寸抖动/随机化噪声)
【测试】test_log_v2 97/97(新增14条规则14断言); 3次全量回归(11文件)稳定
【构建】bundle 5055 行; 推送 7b9ef82

## 2026-09-14 规则15: 未成牌时本手最多诈唬两次 (commit 88c7f1b, 用户规则)
【用户规则】手牌没有与公共牌成牌、采用小额诈唬策略 → 本手最多诈唬两次;
  第三次对手还不弃牌就撤
【实现】
  常量 BLUFF_MAX_PER_HAND=2 / BLUFF_COUNT_MIN_ROUND=1(翻前不计)
  _my_bluff_count(state): 从 history 重放我方下注/加注, 按**当时**公面张数
    (flop3/turn4/river5) 还原当时牌力 → 当时为高牌(未成牌)即计一次诈唬
    · 翻前加注不计(翻前无"与公面成牌"概念)
    · 听牌在牌力口径下也是高牌 → 半诈唬同样计入
    · 只在对手没弃牌的手里累积(对手弃牌这手就结束) → 天然对应"第三次对手
      还不弃牌"语义
  _bluff_cap_guard(state, action): 已达 2 次 + 当前仍未成牌 + raise
    → to_call>0 降级 call / to_call==0 降级 check; 其他动作一律不动
  接入 _decide_impl 出口, 在 _normalize **之前**(降级动作需再过校验),
  在 _doom_call_upgrade 之前(doom 硬规则优先)
【设计决策·关于"撤"】面对下注时降级为**跟注**而非弃牌 —— 规则15 约束的是
  我方主动诈唬(下注/加注), 跟注是响应行为; 且规则14 判定"对手小额下注≈
  诈唬"→ A高跟注 +EV(30%池赔率只需~12%胜率)。若要严格弃牌: 把
  _bluff_cap_guard 返回的 {"act":"call"} 改成 {"act":"fold"}(一行)
【影响面(3局真实日志)】诈唬计数分布 {0:112,1:3} / {0:119,1:1} —— 计数器正常
  但未达上限(这些局没出现"同手空气连开两枪"); 规则15 是安全网待触发
【测试】test_log_v2 112/112(新增15条断言); 3次全量回归(11文件)稳定
【踩坑·git commit 消息】commit -m 的字符串里含 ASCII 双引号(如 {"act":...})
  → 提前终止 shell 字符串 → 消息被截断 + 后续 git push 没执行
  【对策】消息含代码/引号时改用: Write 到 .git/COMMIT_MSG_TMP.txt →
  git commit --amend -F <file>
【构建】bundle 5141 行; 推送 7b9ef82..88c7f1b(amend)

## 2026-09-14 移除 LEAD_LOCK 优势锁定 + 修转换器丢失全下金额 (commit 1827261)
【用户决定】把 leadlock 去掉。理由: 已有"3000 以上谨慎下注"规则(牌型注额分级
  _bet_limit/_bet_cap_guard), 这里是面对 allin 的特殊情况; 且这种情况是后期
  对手持续 allin 赌命; 加注能到 3000 多半还没轮到 allin 且不是前几手
【原规则问题】领先 > LEAD_NO_ALLIN(2000,=单边+1000) 即: ①无论如何不 allin
  ②单次注码 ≤ LEAD_MAX_BET(1000) —— **一刀切**: 即使三条/坚果面对全下也强制
  弃牌。实测 3 局 55 次面对全下全部 fold, 其中 47 次被它拦下
【移除清单】常量 LEAD_NO_ALLIN/LEAD_MAX_BET; 全局 _LEAD_LOCK + _prepare_globals
  刷新; 函数 _is_lead_lock; _allin_floor_guard 的 LEAD_LOCK 分支; _normalize
  三处(any_allin 的领先全下也弃 / fold 分支 not _LEAD_LOCK and doomed /
  allin+raise 分支的 LEAD_MAX_BET 夹紧); _lock_win_unified C 分支前置;
  _doom_call_upgrade 守卫; 5 处注释
【移除后行为(逐项验证)】
  · 翻后三条 / 翻前 AA·KK 在「领先但未锁胜」面对全下 → 跟 ✓
  · 两对/一对/高牌仍受牌型注额上限节制 → 继续弃(3000 规则照管主动下注) ✓
  · 已锁胜(lead>锁赢线) 仍走规则2 锁胜弃牌 ✓
  · 规则2 doom/盈利锁胜、规则16 跟全下门槛 全部照常 ✓
【重大工具 bug 修复】botbattle_log._action_to_history 把 allin 固定写 action=-2
  (**丢掉全下金额**) → game_state 翻后重放把 opp_round_bet 夹到当前最大注 →
  to_call 塌缩成占位 1 → 复盘里所有「面对全下」决策失真
  → 改为与 raise 同口径 `action=amount, action_type='allin'`(缺失才退回 -2)
  → 修复后重放 第30手 to_call 1 → 18603, 三条得以正确跟注
  【教训】之前所有"面对全下 0/55"的量化都基于错误金额, 不可信
【工具同步】audit_lock「领先allin(⚠无依据)」判据过时 → 改为「有 doom/锁胜依据
  或手牌 ≥三条」即 OK, 否则 ⚠需复核
【影响面(3 局, 转换器修复后)】面对全下 55 次: 跟注 旧0 → 新1
  (第30手三条 fold→allin, 对手仅一对2, 跟注本就该赢)
  其余仍 fold 且逐点核对合理: 锁胜弃牌 / 高牌或一对(受注额上限) /
  规则11 公面同花威胁(第38手我顺子 vs 对手 Q♥ 同花, 弃牌正确)
【测试】test_lead_lock.py 按新行为重写(13 条); test_log_v2 128/128;
  test_rule123/test_strategy 各 1 条期望更新; 3 次全量回归(11 文件)稳定
【构建】bundle 5163 行; 推送 88c7f1b..1827261
【git 经验】消息含 ASCII 双引号或长代码片段时, 直接 -m 会被 shell 截断 →
  统一用 Write 到 .git/COMMIT_MSG_TMP.txt + git commit -F

## 2026-09-14 代码答疑: 完整梳理「跟 all-in」判定链(无代码改动)
【结论·五层覆盖顺序】用户问"跟all具体是什么规定", 梳理出权威链条:
  ① decide 入口 _lock_win_unified(规则2, 不看胜率即返回): fold_out→check/fold
     (to_call=0 用 check) / doomed→allin / profit_lock→allin
  ② 门槛: 翻前 _preflop_allin_decide(ALLIN_THR 分档 0.75/0.65/0.55/0.50/0.40,
     ±ALLIN_ENDGAME_SHIFT 0.03, +OPP_JUMP 0.10, 领先者 thr=max(thr,required))
     翻后 thr = eff_req + margin(0.02±) or pressure −0.05 / desperate·doomed −0.08,
     前置「河牌裸公对陷阱 _river_paired_trap」直接 fold
  ③ 规则16 relax: 0.05×h_part(≤8手满/≥45手零) + 0.06×f_part(allin频率/0.25×
     置信 min(1,n/10)), cap 0.09
  ④ _normalize 合法性闸: 翻前 my_left/to_call >1000 非 doomed 且非 _is_super_hand
     → 跟全下转 fold; 翻后 > _bet_limit(小两对2000/<三条3000/≥三条无限) → fold;
     _allin_floor_guard: 累计投入 ≤ twc[我]+1000 → 跟全下转 fold
  ⑤ 出口 _doom_call_upgrade(在 _normalize 之后): 敞口口径 doom 成立时 call/raise
     → 无条件 allin(覆盖牌型上限降级)
  · 触发条件: state.opp_is_allin 或 to_call ≥ my_left(平台只允许 -1/-2)
  · allin_count 是指数衰减计数, 故 freq 反映"近期"全押频率

## 2026-09-14 代码答疑 + 实测: 「盈利锁胜」C 分支的真实场景与遗留瑕疵
【用户问】盈利锁胜是什么场景
【实测脚本结论(已删临时脚本)】_profit_lock_allin(state) 现在**就是** _doom_risk(state):
  原始 lead(不叠加 ctx 偏移) + invested 口径。A 分支(_match_adjust=='doomed')
  用 lead+(_CTX.threshold_offset+strategy_shift)×bb 后判定 → 二者唯一差别在此。
  · ctx 偏移 ≤0(激进档) → A 先命中, C 完全冗余(死代码)
  · ctx 偏移 >0(保守/保收益档) → A 因 lead 抬高不触发, C 用原始 lead 仍触发
    **这是 C 唯一实际生效窗口**(实测 lead=+1000/invested=1150/le=0/ctx=+5BB:
     A=normal, C=True)
【触发门槛实测(lead=+1000, 位置: 我小盲)】剩69手需已投≥5650 / 剩39手≥3400 /
  剩19手≥1900 / 剩9手≥1150 / 剩5手≥850 / 剩1手≥550 (追平线 5150/2900/1400/650/350/50)
  即"盈利 + 本局投入巨大(输掉这局对手就锁赢)"→ 强制 allin 锁定
【发现·疑似瑕疵(未修, 待用户决定)】C 返回 allin 后 decide 走 _normalize(state, _lock_win_legal(...)),
  而 _lock_win_legal 只管 fold→check; _normalize 的 allin 分支用 _over_limit/_bet_limit,
  此时 _match_adjust **不是 doomed**(ctx 正偏移) → 翻前 my_left>1000 非超强牌 → to_call>0 转 fold;
  翻后 my_left > _bet_limit(小两对2000/其余<三条3000) → 跟全下转 fold(主动降为上限加注)。
  ⇒ C 想"无条件 allin"但在该窗口会被牌型上限降级。修法候选: ①让 C 分支也豁免
  (_lock_win_legal 里对 profit_lock 的 allin 免检) ②直接删掉 C(承认只保留 A 的 ctx 敏感口径)
  ③把 C 改成传 include_to_call 口径统一

## 2026-09-14 【已实施】方案1 + 替换所有对 allin 的旧限制 (commit ccaf8ae)
【用户决定】采用方案1; 并明确「针对对手 allin 的定向修改, 替换所有之前
  对 allin 的限制」
【实现 1·锁赢免检】_lock_win_legal(state, action): act=='allin' 时给 action
  副本加内部标记 lk=1; _normalize 开头算 lk_allin = bool(action.get('lk'))
  and act=='allin' → any_allin 分支最前 + act=='allin' 分支 → 直接放行。
  两个锁赢调用点统一走 _lock_win_legal(decide 入口已有; _decide_impl 的
  `return _normalize(state, _lw)` 改为 `_normalize(state, _lock_win_legal(state, _lw))`)
  · 标记不外泄: _normalize 返回的都是新建 dict
【实现 2·替换旧限制(用户规则)】_normalize 删除两条针对 allin 的旧限制:
  · any_allin 分支: 原「_over_limit(my_left)→fold」与「翻后 my_left>
    _bet_limit→fold」整条删除 → 非 fold 一律 allin
  · act=='allin' 分支: 「to_call>=my_left(跟注即全下) → 放行 allin」;
    主动 shove(to_call<my_left) 仍降级为上限加注(保留)
  · act=='call' 且 to_call>=my_left: 原两条检查删除 → 直接 allin
  语义: 面对对手 all-in 的定向决策(规则2/规则16/_preflop_allin_decide/
  翻后全下分支)说了算, 不再被金额/牌型上限二次推翻
【行为变化(实测 to_call 真实金额)】AA 大幅领先+6000→fold(领先保护, thr0.75);
  KK 小幅领先→allin; TT 均势→fold(突袭 _OPP_JUMPED 把 0.55 抬到 0.65);
  72o 均势→fold; A5s/72o 大幅落后→allin(搏翻盘); 72o 翻后面对真实全下→fold
【测试修正(3 处失败都是"测试构造失真"而非策略错)】
  · action=-2 会让 to_call 塌缩为占位 1 → 赔率失真 → 弱牌也判跟注。
    【重要发现】game_state 对 allin 金额的解析: 写 action=真实金额 才得到
    正确 to_call; 写 -2 时翻后 to_call=1(翻前则是按对手筹码推算)。
    已修: test_strategy.allin_req / test_preflop_cap E4 / test_lead_lock 场景2
  · TT 均势+突袭全下 eq≈0.65 正好落在门槛上(MC 600 次抽样即翻转) →
    不能做确定性断言 → 改为直测 _normalize(确定性) + 保留 72o 弱牌断言
  · 删除 test_preflop_cap「翻前均势TT大额全下→弃(2026-08-24)」断言
【文档同步】代码逐行详解.md: §4.11 _normalize 重写(新增 lk 说明),
  §4.3 改为「LEAD_LOCK 已移除」, 调用图去掉 _is_lead_lock;
  程序运行逻辑图.html: 2 处 _LEAD_LOCK 残留改为 _OPP_JUMPED/_OPP_BETS_PER_HAND
【验证】11 文件 × 4 轮全量回归全绿; bundle 5201 行(5163→5201); 推送 ccaf8ae

## 2026-09-14 【重大修复】doom 受 ctx 偏移污染 + 规则10 强化 (commit ff7509d)
【用户截图·第66手】翻牌 9♣5♥8♠, 我方(测01=BB) 10♠7♦ 高牌/两头顺听牌,
  对手(ronghe2=SB)加注至343 → 我方 allin 19900。用户:「这里没牌怎么能先手all?」
【牌面/资金还原(截图推断)】我方 20000→0(已投100翻前); 对手 20000→19557
  (已投100+343); 终池 20443 = 双方总投入和; lead=+134(本场+67双边);
  invested=100, 剩 4 手, 锁赢线 800
【根因(已实测复现)】_match_adjust 的 doom 判定所传 lead 被 ctx 偏移污染:
  doom_raw(原始 lead)=False(弃牌只损失100, 安全) 但 ctx=-6BB → lead=-466 →
  adjust=doomed → 规则2 无条件 allin(且 doomed 使 _bet_limit 无限 → 不被降级)。
  ctx=-4/-5BB 仍 normal, -6/-8BB 变 doomed。
【修复1·_match_adjust】doom 判定改用 lead_raw(不叠加 ctx 偏移); ctx 偏移只
  作用于 protect/pressure/desperate 阈值。⇒ 同场景 ctx=-6/-8BB 都 normal →
  常规「大注弃牌」→ fold ✓(保留 +134 领先)
【修复2·规则10 强化(用户规则:靠近锁赢线不要主动加注,尽量跟注和过牌)】
  · 常量 STABILITY_LINE_FACTOR=0.60(原硬编码 0.8) / STABILITY_ENDGAME_HANDS=8
  · _stability_mode: 条件2 用 0.6×锁赢线; 新增条件3「剩≤8手 且 lead>0」
  · _check_side_stable: 取消「对手过牌→强制小注(规则12)」→ 一律 check
  · 新增 _stability_guard(接在 _bluff_cap_guard 之后、_normalize 之前):
    求稳时 raise/allin → to_call==0 转 check; 0<to_call<my_left 转 call;
    to_call≥my_left 保留(跟注即全下); 强牌例外(翻后有效牌型≥两对/翻前超强牌)
【测试】test_log_v2: M4 两条断言按新行为重写(改用 30BB=3000 protect 边界验证
  ctx 生效; 新增 doom 两档一致 + 不误 allin); 新增规则10 8 条断言;
  K9o 边缘断言改 K6o(原断言 MC 噪声会偶发失败, 5 次验证稳定)。11 文件×3 轮全绿
【文档】程序运行逻辑图.html:「doomed:同一不等式 + _CTX」→「doomed:原始 lead」
【构建】bundle 5266 行; 推送 ff7509d
【经验】确定性硬规则(doom/锁赢)的判定输入必须用客观原始值 —— 任何"赛制/风格
  偏移"只能改软阈值, 否则会把「弃牌安全」误判成「必须搏命」。

## 2026-09-14 【修复】跟全下豁免全下下限 + 葫芦误判弱两对 (commit dd24755)
【用户截图·第23手】翻牌 2♠J♣J♦, 我方(BB) Q♦T♦, 对手(座位2)全押 19900
  (终池 20100) → 我方弃牌。用户问「这为啥弃了」
  · 该手弃牌**正确**: 需跟 19900、池 20100 → required=49.75%；
    Q♦T♦ 在 J J 2 面只有一对 J + Q 踢脚(eq≈44~48%) → 低于门槛 ✓
    归因日志: 规则1/4 大注弃牌 (to_call > 0.6×pot 且无坚果/强听)
【顺链排查出两个缺陷(都不是这手的问题, 是它暴露的)】
  1) _allin_floor_guard 会拦「跟对手全下」: 实测 手牌 JJ + 公面 2♠J♣J♦ = 四条,
     对手下注 3000 → **fold**(invest=3100 < floor=twc[我]+1000=4468) 必胜牌被弃
     · 修复: any_allin 或 to_call ≥ my_left → 直接放行(用户 2026-09-14「面对
       对手 all-in 的定向决策替换所有旧限制」); 主动 shove 仍受本下限约束
  2) should_avoid_risk 把葫芦判成「弱两对」: 手牌 22 + 公面 J J 2 = 葫芦(222JJ),
     旧条件「手牌对(2) < 公对(J) 且 kicker(0) < 公面高张(2)」→ True → 走
     _risk_avoid_route 保守路线 → 弃牌
     · 修复: _effective_category ≥ THREE_OF_A_KIND 一律豁免
【修复后实测】葫芦/四条 面对全押或大注 → allin(此前 fold); 一对(含截图手)
  仍 fold; 两对面对突袭仍 fold(用户 2026-09-04 规则保留, 不动)
【测试】4 条「全下下限」断言按新行为重写并标注豁免范围; test_preflop_cap F1
  构造改真实全下金额(19000); 新增「葫芦(222JJ)不触发弱两对规避」。11×3 全绿
【构建】bundle 5289 行; 推送 dd24755; Actions success
