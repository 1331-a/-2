# 2026-08-26 工作日志

## 三项规则改动（commit 1a55147，用户规则）

### 1. 注额分级限制（取代旧 2000 上限）
- **HIGH_BET_LIMIT 2000→3000**：牌型 <三条 总注额（跟注/allin/raise 统一口径）≤3000；
- **新增 SMALL_TWO_PAIR_LIMIT=2000**：小两对 ≤2000。判定 `_is_small_two_pair`：
  两对中最大对 ≤9（小牌两对）或 should_avoid_risk 弱两对（公对+踢脚被压制）；
- **≥三条（_effective_category 净化后）/ doomed 不限**（可 allin）；
- 新增 `_bet_limit(state)`（当前牌型注额上限）与 `_over_limit(state, amount)`
  （翻前 1000 / 翻后 _bet_limit，自动按 stage 区分——修过 bug：翻后误走翻前判断致强牌 fold）；
- 规则3 BET_CAP 1000→3000（主动下注克制让位于分级上限）。

### 2. doom 无条件 allin（删除主动侧 steal 施压分流）
- 2026-08-25 的「被动 allin / 主动 steal 施压」分流取消：确定本局失败对手锁赢
  （确定性 doom 公式）→ **无条件 allin**（无论主动/被动，弃牌=认输）。
- _match_adjust 不再返回 "steal"；decide 入口 doomed→allin 覆盖一切。
- 决策层的 `adj == "steal"` 分支变死代码（保留未删，防御性）。

### 3. 策略学习（记录对手下注数量 + 赢牌策略）
- **opponent.py**：`cur_hand_bets`（当前手对手 bet/raise 次数）+ `hand_bet_counts`
  （最近 12 手 FIFO）→ `avg_bets_per_hand()`（样本<3 向先验 0.9 收缩）；
  _face_bet 跟注门槛微调：≥1.5 次/手（高侵略）收紧 +3%，≤0.7（被动）放宽 -2%。
- **match_ctx.py**：`_tag_from_history` 按我方翻后主动下注次数分策略标签
  aggro（≥2 或全下）/ cbet（1 次）/ passive（0 次）；`strat_stats` 每手按
  标签+胜负（net>0 赢/<0 输/0 平忽略）累计；`strategy_shift()` 胜率>0.62 且
  样本≥5 → ±1BB 偏移，叠加到 _match_adjust 的 lead（与 threshold_offset 并列）。

## 测试与验证
- 全量回归 ×3 稳定（10 个测试文件）。
- 新增：test_match_ctx 策略标签 4 项 + 偏移 4 项；test_bet_resp 每局下注 4 项；
  test_rule123 4-13 改 _bet_limit 分级断言（doomed 不限/普通<三条 3000/真四条不限/
  小两对 2000）；规则1/施压档改无条件 allin 断言。
- 冒烟：主动 doomed→-2；顶对 A 面对 1500→0（≤3000 跟注）；小两对面对 2500→-1
  （超 2000 弃）；真四条面对 2500→-2 ✓。bundle 3372 行。

## 发布
- commit 1a55147，10 文件。**测/poker_bot 未提交**（用户自行替换的新 ELF，
  与本次改动无关）。
- 推送遇网络阻断（直连+代理均不可达），后台循环重试中。
- **推送成功**：用户开启代理（7897）→ 后台探测自动推送 7733586..1a55147。
- Actions build-elf：1a55147 completed success ✅。

## 追加规则（commit 70c6b83）：小两对以下也限 2000
- 用户规则：「小两对以下都只能下 2000 以下」。
- _bet_limit 分级更新：一对/高牌/小两对（cat<TWO_PAIR 或小两对）→ 2000；
  仅大两对（非小两对）保持 3000；≥三条/doomed 不限。
- 4-13 增一对=2000、高牌=2000、大两对=3000（K-Q 两对）断言；全量回归×3。
- 冒烟：顶对 A 面对 2500→弃牌、大两对 K-Q 面对 2500→跟注 ✓。
- 推送成功（代理仍开）：1a55147..70c6b83；Actions 70c6b83 completed success ✅。

## 2026-08-27 用户手改 strategy.py（commit bc54f58）
- **DECISION_TIMEOUT=0.90s 决策硬超时保护**：decide 入口记录 perf_counter 起始时间，
  `_decision_timed_out()` 在规则2 等关键点兜底返回 fold，防极端耗时超时。
- **_opp_bet_jumped 重写**：原「对手累计投入>2000 即触发」改为「单次加注增量
  > 4×此前累计(首次>2000) 才判定突袭大注」（OPP_JUMP_RATIO=4.0）——
  正常节奏的大注不再误触发收紧跟注。
- 语法 OK（py_compile）、全量回归×3 稳定、bundle 3390 行、冒烟通过。
- 推送：直连被阻断 → 用户开代理（7897）→ 自动推送 70c6b83..bc54f58；
  Actions bc54f58 completed success ✅。
